---
phase: 11-mcp-tools-photo-proxy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - chat_app/graph_client.py
  - exchange_mcp/tools.py
autonomous: true

must_haves:
  truths:
    - "get_user_profile() returns user detail fields plus manager displayName in a single Graph API call"
    - "get_user_photo_96() returns 96x96 photo bytes or None (no exception) when user has no photo"
    - "search_colleagues and get_colleague_profile tool schemas are present in TOOL_DEFINITIONS"
  artifacts:
    - path: "chat_app/graph_client.py"
      provides: "get_user_profile and get_user_photo_96 functions"
      contains: "def get_user_profile"
    - path: "chat_app/graph_client.py"
      provides: "96x96 photo endpoint function"
      contains: "def get_user_photo_96"
    - path: "exchange_mcp/tools.py"
      provides: "search_colleagues tool definition schema"
      contains: 'name="search_colleagues"'
    - path: "exchange_mcp/tools.py"
      provides: "get_colleague_profile tool definition schema"
      contains: 'name="get_colleague_profile"'
  key_links:
    - from: "chat_app/graph_client.py"
      to: "Graph API /users/{id}"
      via: "get_user_profile with $expand=manager"
      pattern: "expand.*manager"
    - from: "chat_app/graph_client.py"
      to: "Graph API /photos/96x96/$value"
      via: "get_user_photo_96"
      pattern: "photos/96x96"
---

<objective>
Add two new functions to graph_client.py (get_user_profile, get_user_photo_96) and register two new MCP tool definition schemas in tools.py.

Purpose: Provides the Graph API data layer and MCP schema foundation that handlers (Plan 11-02) and photo proxy (Plan 11-03) will consume.
Output: Extended graph_client.py with profile + 96x96 photo functions; tools.py TOOL_DEFINITIONS extended with search_colleagues and get_colleague_profile schemas.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mcp-tools-photo-proxy/11-CONTEXT.md
@.planning/phases/11-mcp-tools-photo-proxy/11-RESEARCH.md
@chat_app/graph_client.py
@exchange_mcp/tools.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add get_user_profile() and get_user_photo_96() to graph_client.py</name>
  <files>chat_app/graph_client.py</files>
  <action>
    Add two new public functions after the existing `get_user_photo_bytes()` function:

    1. `get_user_profile(user_id: str) -> dict | None`:
       - Early return None if `not _graph_enabled or not user_id`
       - Get headers via `_make_headers()` (no search=True needed)
       - GET `{Config.GRAPH_BASE_URL}/users/{user_id}` with params:
         - `$select`: `id,displayName,mail,jobTitle,department,officeLocation,businessPhones`
         - `$expand`: `manager($select=displayName)`
       - Use `_graph_request_with_retry("GET", url, headers=headers, params=params)`
       - Return None on 404 status code
       - Call `resp.raise_for_status()` for other errors
       - Return `resp.json()` on success
       - Wrap in try/except, log error, return None on any exception

    2. `get_user_photo_96(user_id: str) -> bytes | None`:
       - Identical pattern to existing `get_user_photo_bytes()` but uses endpoint `/users/{user_id}/photos/96x96/$value` instead of `/users/{user_id}/photo/$value`
       - Early return None if `not _graph_enabled or not user_id`
       - Get headers via `_make_headers()`
       - Use `_graph_request_with_retry`
       - Return None on 404 (no photo — silent, no log)
       - Return `resp.content` on success
       - Wrap in try/except, log error, return None

    Both functions follow the exact same defensive patterns as existing `search_users()` and `get_user_photo_bytes()`.
  </action>
  <verify>Run `python -c "from chat_app.graph_client import get_user_profile, get_user_photo_96; print('imports OK')"` to confirm the functions are importable. Run `python -m py_compile chat_app/graph_client.py` to verify syntax.</verify>
  <done>graph_client.py exports get_user_profile and get_user_photo_96 with correct signatures, both using _graph_request_with_retry and returning None on errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add search_colleagues and get_colleague_profile tool definitions to TOOL_DEFINITIONS</name>
  <files>exchange_mcp/tools.py</files>
  <action>
    Append two new `types.Tool(...)` entries to the TOOL_DEFINITIONS list in exchange_mcp/tools.py, before the closing bracket. Add them in a new section comment block:

    ```
    # ------------------------------------------------------------------
    # Colleague lookup tools (Phase 11 — Graph API)
    # ------------------------------------------------------------------
    ```

    1. `search_colleagues` tool definition:
       - name: "search_colleagues"
       - description: LLM-oriented hint — "Search for colleagues by name or email address. Use for name lookups like 'find John Smith' or 'who is alice@company.com'. Returns up to 10 results with name, job title, department, and email. Use get_colleague_profile for detailed info or photo."
       - inputSchema: object with one required property `query` (type: string, description: "Name or email address to search for")

    2. `get_colleague_profile` tool definition:
       - name: "get_colleague_profile"
       - description: LLM-oriented hint — "Get detailed profile for a specific colleague including photo URL. Use when you have a user ID from search_colleagues results and need full details like office location, phone numbers, manager, or photo."
       - inputSchema: object with one required property `user_id` (type: string, description: "The colleague's user ID from search results")

    Also update the module docstring to say "17 mcp.types.Tool objects (14 Exchange + ping + 2 Graph)" instead of "15 mcp.types.Tool objects (14 Exchange + ping)".

    Do NOT add handlers or TOOL_DISPATCH entries yet — that is Plan 11-02.
  </action>
  <verify>Run `python -c "from exchange_mcp.tools import TOOL_DEFINITIONS; print(f'{len(TOOL_DEFINITIONS)} tools'); names = [t.name for t in TOOL_DEFINITIONS]; assert 'search_colleagues' in names; assert 'get_colleague_profile' in names; print('OK')"` to confirm 17 tools with correct names.</verify>
  <done>TOOL_DEFINITIONS contains 17 tool entries including search_colleagues and get_colleague_profile with correct inputSchema definitions.</done>
</task>

</tasks>

<verification>
- `python -m py_compile chat_app/graph_client.py` passes
- `python -m py_compile exchange_mcp/tools.py` passes
- `from chat_app.graph_client import get_user_profile, get_user_photo_96` succeeds
- `from exchange_mcp.tools import TOOL_DEFINITIONS; len(TOOL_DEFINITIONS) == 17`
- search_colleagues and get_colleague_profile tool names present in TOOL_DEFINITIONS
</verification>

<success_criteria>
- graph_client.py has get_user_profile() using $expand=manager($select=displayName)
- graph_client.py has get_user_photo_96() using /photos/96x96/$value endpoint
- Both new functions follow existing defensive patterns (None on error, no exceptions to caller)
- tools.py TOOL_DEFINITIONS has 17 entries with search_colleagues and get_colleague_profile schemas
- All files compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/11-mcp-tools-photo-proxy/11-01-SUMMARY.md`
</output>
