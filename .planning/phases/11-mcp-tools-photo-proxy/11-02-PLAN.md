---
phase: 11-mcp-tools-photo-proxy
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - exchange_mcp/tools.py
  - exchange_mcp/server.py
autonomous: true

must_haves:
  truths:
    - "search_colleagues tool returns up to 10 results with name, jobTitle, department, email — no user ID in results"
    - "search_colleagues returns a human-readable 'no results' message string when no matches found"
    - "get_colleague_profile returns profile fields with falsy values omitted plus a photo_url string"
    - "get_colleague_profile photo_url is /api/photo/{user_id} — binary photo data never enters tool result"
    - "Both handlers use asyncio.to_thread to avoid blocking the MCP event loop"
    - "Graph client import happens inside handler functions, not at module scope"
  artifacts:
    - path: "exchange_mcp/tools.py"
      provides: "_search_colleagues_handler async function"
      contains: "_search_colleagues_handler"
    - path: "exchange_mcp/tools.py"
      provides: "_get_colleague_profile_handler async function"
      contains: "_get_colleague_profile_handler"
    - path: "exchange_mcp/tools.py"
      provides: "TOOL_DISPATCH entries for both new tools"
      contains: '"search_colleagues": _search_colleagues_handler'
  key_links:
    - from: "exchange_mcp/tools.py"
      to: "chat_app/graph_client.py"
      via: "import inside handler + asyncio.to_thread"
      pattern: "asyncio\\.to_thread"
    - from: "exchange_mcp/tools.py"
      to: "TOOL_DISPATCH dict"
      via: "handler registration"
      pattern: "search_colleagues.*_search_colleagues_handler"
---

<objective>
Implement the async handler functions for search_colleagues and get_colleague_profile, wire them into TOOL_DISPATCH, and update server.py tool count comment.

Purpose: Makes both MCP tools callable end-to-end — the LLM can now invoke search_colleagues and get_colleague_profile through the tool-calling loop.
Output: Working tool handlers with correct data shaping (strip IDs from search, omit falsy profile fields, include photo_url), registered in TOOL_DISPATCH.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mcp-tools-photo-proxy/11-CONTEXT.md
@.planning/phases/11-mcp-tools-photo-proxy/11-RESEARCH.md
@.planning/phases/11-mcp-tools-photo-proxy/11-01-SUMMARY.md
@exchange_mcp/tools.py
@exchange_mcp/server.py
@chat_app/graph_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement _search_colleagues_handler and _get_colleague_profile_handler</name>
  <files>exchange_mcp/tools.py</files>
  <action>
    Add `import asyncio` at the top of tools.py (after existing imports, before TYPE_CHECKING block).

    Add two handler functions BEFORE the TOOL_DISPATCH dict (after _ping_handler, before _make_stub):

    1. `async def _search_colleagues_handler(arguments: dict[str, Any], client: ExchangeClient | None) -> dict[str, Any]`:
       - Extract `query = arguments.get("query", "").strip()`
       - Return `{"message": "Please provide a search query."}` if query is empty
       - Import INSIDE the function body (not at module scope — avoids import-time Config eval):
         `from chat_app.graph_client import is_graph_enabled, search_users`
       - Check `is_graph_enabled()` — return `{"message": "Colleague search is not available (Graph API not configured)."}` if false
       - Call `raw = await asyncio.to_thread(search_users, query)`
       - If `not raw`: return `{"message": f"No colleagues found matching '{query}'."}`
       - Slice to max 10: `raw[:10]`
       - Map each result to dict with ONLY these keys (omit falsy values):
         - "name" from "displayName"
         - "jobTitle" from "jobTitle"
         - "department" from "department"
         - "email" from "mail"
       - Deliberately EXCLUDE "id" from results (CONTEXT.md decision: no ID in search results)
       - Return `{"results": results, "count": len(results)}`

    2. `async def _get_colleague_profile_handler(arguments: dict[str, Any], client: ExchangeClient | None) -> dict[str, Any]`:
       - Extract `user_id = arguments.get("user_id", "").strip()`
       - Return `{"message": "Please provide a user_id."}` if empty
       - Import INSIDE the function body:
         `from chat_app.graph_client import is_graph_enabled, get_user_profile`
       - Check `is_graph_enabled()` — return `{"message": "Colleague profiles are not available (Graph API not configured)."}` if false
       - Call `raw = await asyncio.to_thread(get_user_profile, user_id)`
       - If `raw is None`: return `{"message": f"No profile found for user ID '{user_id}'."}`
       - Build profile dict, omitting keys where value is falsy (None, "", []):
         - "name" from raw["displayName"]
         - "email" from raw["mail"]
         - "jobTitle" from raw["jobTitle"]
         - "department" from raw["department"]
         - "officeLocation" from raw["officeLocation"]
         - "businessPhones" from raw["businessPhones"] (only if non-empty list)
         - "manager" from raw.get("manager", {}).get("displayName") (only if truthy)
       - ALWAYS add `"photo_url": f"/api/photo/{user_id}"` (even if user has no photo — proxy handles fallback)
       - Return the profile dict directly (not wrapped in {"result": ...})

    Then add both handlers to the TOOL_DISPATCH dict:
    ```python
    "search_colleagues": _search_colleagues_handler,
    "get_colleague_profile": _get_colleague_profile_handler,
    ```
  </action>
  <verify>Run `python -m py_compile exchange_mcp/tools.py` to verify syntax. Run `python -c "from exchange_mcp.tools import TOOL_DISPATCH; assert 'search_colleagues' in TOOL_DISPATCH; assert 'get_colleague_profile' in TOOL_DISPATCH; print(f'{len(TOOL_DISPATCH)} handlers OK')"` to confirm dispatch registration.</verify>
  <done>Both handlers are implemented with correct data shaping (no IDs in search, falsy values omitted from profile, photo_url always present), use asyncio.to_thread for sync graph_client calls, and are registered in TOOL_DISPATCH.</done>
</task>

<task type="auto">
  <name>Task 2: Update server.py tool count comment</name>
  <files>exchange_mcp/server.py</files>
  <action>
    Update the module docstring in server.py: change "15 registered tools (14 Exchange tools + ping)" to "17 registered tools (14 Exchange + ping + 2 Graph colleague tools)".

    Also update any other comments in server.py that reference the tool count of 15, changing them to 17.
  </action>
  <verify>Run `grep -n "15\|17" exchange_mcp/server.py` to confirm all count references updated. Run `python -m py_compile exchange_mcp/server.py` to verify syntax.</verify>
  <done>server.py comments reflect 17 tools.</done>
</task>

</tasks>

<verification>
- `python -m py_compile exchange_mcp/tools.py` passes
- `python -m py_compile exchange_mcp/server.py` passes
- TOOL_DISPATCH has 17 entries (15 existing + 2 new)
- Both handlers use `asyncio.to_thread` (grep confirms)
- Both handlers import graph_client inside function body (grep confirms no module-scope import of chat_app.graph_client)
- search_colleagues handler strips "id" from results (code inspection)
- get_colleague_profile handler omits falsy keys and always includes photo_url
</verification>

<success_criteria>
- search_colleagues handler returns max 10 results with {name, jobTitle, department, email} only
- search_colleagues returns human-readable "No colleagues found" message on empty results
- get_colleague_profile returns profile dict with falsy values omitted + photo_url always present
- Both handlers gracefully degrade when Graph is not configured
- asyncio.to_thread wraps all synchronous graph_client calls
- TOOL_DISPATCH maps both tool names to their handlers
- server.py comments updated to 17 tools
</success_criteria>

<output>
After completion, create `.planning/phases/11-mcp-tools-photo-proxy/11-02-SUMMARY.md`
</output>
