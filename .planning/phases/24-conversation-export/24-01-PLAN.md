---
phase: 24-conversation-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/utils/exportMarkdown.ts
  - frontend/src/utils/slugify.ts
  - frontend/src/utils/downloadBlob.ts
  - frontend/src/components/ChatPane/ExportMenu.tsx
  - frontend/src/components/ChatPane/Header.tsx
autonomous: true

must_haves:
  truths:
    - "Export button appears in ChatPane header when a thread has messages"
    - "Clicking Markdown menu item downloads a .md file with full conversation including tool data"
    - "Downloaded filename contains slugified thread name and current date"
    - "Export button is disabled during streaming and when thread has no messages"
    - "Export operates solely on client-side data already fetched through ownership-verified API endpoints -- no thread ID enumeration or unauthorized access is possible (inherits EXPT-04 thread ownership guarantee)"
  artifacts:
    - path: "frontend/src/utils/exportMarkdown.ts"
      provides: "Pure function converting DisplayMessage[] to Markdown string"
      exports: ["messagesToMarkdown"]
    - path: "frontend/src/utils/slugify.ts"
      provides: "Filename generation with slugified thread name and date"
      exports: ["slugify", "exportFilename"]
    - path: "frontend/src/utils/downloadBlob.ts"
      provides: "Client-side Blob download utility"
      exports: ["downloadBlob"]
    - path: "frontend/src/components/ChatPane/ExportMenu.tsx"
      provides: "Fluent UI Menu with Markdown format option"
      exports: ["ExportMenu"]
    - path: "frontend/src/components/ChatPane/Header.tsx"
      provides: "Header with integrated export menu"
  key_links:
    - from: "frontend/src/components/ChatPane/ExportMenu.tsx"
      to: "frontend/src/utils/exportMarkdown.ts"
      via: "onExportMarkdown callback wired in Header"
      pattern: "messagesToMarkdown"
    - from: "frontend/src/components/ChatPane/Header.tsx"
      to: "frontend/src/contexts/ChatContext.tsx"
      via: "useChat hook for messages and isStreaming"
      pattern: "useChat"
    - from: "frontend/src/components/ChatPane/Header.tsx"
      to: "frontend/src/contexts/ThreadContext.tsx"
      via: "useThreads hook for thread name lookup"
      pattern: "useThreads"
    - from: "frontend/src/utils/slugify.ts"
      to: "frontend/src/utils/downloadBlob.ts"
      via: "exportFilename used to generate filename passed to downloadBlob"
      pattern: "exportFilename.*downloadBlob"
---

<objective>
Add Markdown export to the ChatPane so IT engineers can download the active thread as a .md file for pasting into Jira/incident reports.

Purpose: EXPT-01 through EXPT-04 -- client-side Markdown export with tool panel data, Fluent Menu trigger, slugified filename with date, and zero server round-trip.
Output: Three utility modules (exportMarkdown, slugify, downloadBlob), an ExportMenu component, and an updated Header with the export button wired in.
</objective>

<execution_context>
@C:\Users\taylo\.claude/agents/gsd-planner.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-conversation-export/24-CONTEXT.md
@.planning/phases/24-conversation-export/24-RESEARCH.md
@frontend/src/types/index.ts
@frontend/src/contexts/ChatContext.tsx
@frontend/src/contexts/ThreadContext.tsx
@frontend/src/components/ChatPane/Header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create export utility functions</name>
  <files>
    frontend/src/utils/exportMarkdown.ts
    frontend/src/utils/slugify.ts
    frontend/src/utils/downloadBlob.ts
  </files>
  <action>
Create three pure utility modules:

**exportMarkdown.ts** -- `messagesToMarkdown(messages: DisplayMessage[], threadName: string, exportDate: string): string`
- Import `DisplayMessage` and `ToolPanelData` from `../types`
- Output structure: H1 thread name, export date line, horizontal rule, then message turns
- User turns: `## User` heading, content, horizontal rule
- Assistant turns: `## Assistant` heading, tool panels BEFORE content (mirrors UI order), content, horizontal rule
- Tool panel formatting via private `formatToolPanel(tool: ToolPanelData): string`:
  - `### Tool: {name} ({status}, {elapsed}ms)` -- elapsed computed from `(endTime - startTime) * 1000`, omit timing if either is null
  - Parameters in fenced JSON code block (skip if empty object)
  - Result in fenced JSON code block (pretty-print if valid JSON, raw string otherwise; skip if null)
- Return `lines.join('\n')`

**slugify.ts** -- two exports:
- `slugify(text: string): string` -- lowercase, trim, strip non-word chars except spaces/hyphens, spaces/underscores to hyphens, collapse multiple hyphens, trim leading/trailing hyphens
- `exportFilename(threadName: string, date?: Date): string` -- returns `{slug}-{YYYY-MM-DD}.md` using `date.toISOString().slice(0,10)`, defaults to `new Date()`

**downloadBlob.ts** -- `downloadBlob(content: string, filename: string, mimeType?: string): void`
- Default mimeType `'text/markdown'`
- Create Blob with `{type: mimeType + ';charset=utf-8'}`
- Create object URL, create anchor element, set href and download attribute, append to body, click
- `setTimeout(() => { removeChild(a); revokeObjectURL(url); }, 0)` for cleanup

All three modules are pure (no React imports, no side effects except downloadBlob's DOM manipulation). Follow existing utils style -- see `formatTimestamp.ts` and `groupThreadsByRecency.ts` for conventions.
  </action>
  <verify>
Run `npx tsc --noEmit` from `frontend/` -- no type errors in the new files. Manually verify each file exports the documented functions by checking the compiled output or import resolution.
  </verify>
  <done>
Three utility files exist with correct type signatures. `messagesToMarkdown` produces valid Markdown with tool panels in fenced code blocks. `exportFilename` generates `slug-YYYY-MM-DD.md` format. `downloadBlob` creates and cleans up object URLs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ExportMenu component and wire into Header</name>
  <files>
    frontend/src/components/ChatPane/ExportMenu.tsx
    frontend/src/components/ChatPane/Header.tsx
  </files>
  <action>
**ExportMenu.tsx** -- new component:
- Import `Menu`, `MenuTrigger`, `MenuPopover`, `MenuList`, `MenuItem`, `MenuButton` from `@fluentui/react-components`
- Import `ArrowDownloadRegular` from `@fluentui/react-icons`
- Props: `{ onExportMarkdown: () => void; disabled?: boolean }`
- Render Fluent Menu with MenuButton (appearance="subtle", icon=ArrowDownloadRegular, aria-label="Export conversation", text "Export")
- Single MenuItem "Markdown (.md)" calling `onExportMarkdown`
- Add a comment `{/* Future: JSON format from EXPT-05 */}` after the MenuItem for future extensibility

**Header.tsx** -- modify existing:
- Add imports: `useChat` from ChatContext, `useThreads` from ThreadContext
- Add imports: `messagesToMarkdown` from utils/exportMarkdown, `exportFilename` from utils/slugify, `downloadBlob` from utils/downloadBlob
- Add import: `ExportMenu` from ./ExportMenu
- Extract from hooks: `const { messages, isStreaming } = useChat()` and `const { threads, activeThreadId } = useThreads()`
- Compute `exportDisabled = isStreaming || messages.length === 0`
- Create `handleExportMarkdown` function:
  - Look up thread name: `threads.find(t => t.id === activeThreadId)?.name ?? 'conversation'`
  - Generate date string: `new Date().toISOString().slice(0, 10)` for display in Markdown (ISO format matches the filename date for consistency)
  - Call `messagesToMarkdown(messages, threadName, dateStr)`
  - Call `downloadBlob(markdown, exportFilename(threadName))`
- Add `<ExportMenu onExportMarkdown={handleExportMarkdown} disabled={exportDisabled} />` in the header element, between the user-info span and the theme toggle button
- Keep existing Header props interface and all current functionality intact
  </action>
  <verify>
Run `npx tsc --noEmit` from `frontend/` -- no type errors. Run `npm run build` from `frontend/` -- production build succeeds. Verify the ExportMenu component renders by checking the build output includes the Menu imports.
  </verify>
  <done>
ExportMenu renders a Fluent MenuButton with ArrowDownloadRegular icon. Clicking "Markdown (.md)" triggers client-side Blob download of the full conversation as a .md file. Button is disabled during streaming and when messages array is empty. Filename follows `{slug}-{YYYY-MM-DD}.md` pattern.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build frontend dist and verify</name>
  <files>
    frontend/dist/
  </files>
  <action>
Run `npm run build` from `frontend/` to produce the production dist that Flask serves.

After build succeeds, do a quick sanity check:
- Verify the built JS bundle includes "Export" string (grep the output JS)
- Verify the built JS bundle includes "ArrowDownload" or the icon reference
- Verify no build warnings related to the new files

This ensures the pre-built dist served by Flask includes the export feature.
  </action>
  <verify>
`npm run build` exits 0. `grep -r "Export" frontend/dist/assets/*.js` finds matches. The dist directory contains updated asset files with recent timestamps.
  </verify>
  <done>
Frontend dist is rebuilt with export feature included. Flask will serve the updated bundle on next restart.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` in frontend/ passes with zero errors
2. `npm run build` in frontend/ succeeds
3. Built dist contains export-related code
4. All five source files exist at their documented paths
5. No new dependencies added to package.json (zero new packages)
</verification>

<success_criteria>
- EXPT-01: messagesToMarkdown includes tool panel data (name, params, result, status, timing) in fenced code blocks
- EXPT-02: ExportMenu renders in Header with Fluent Menu offering Markdown format choice
- EXPT-03: exportFilename produces `{slugified-name}-{YYYY-MM-DD}.md` format
- EXPT-04: Entire flow is client-side Blob download -- no fetch calls, no server endpoints
- Export disabled during streaming (isStreaming) and when no messages exist
</success_criteria>

<output>
After completion, create `.planning/phases/24-conversation-export/24-01-SUMMARY.md`
</output>
