---
phase: 23-thread-search
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - frontend/src/hooks/useDebounce.ts
  - frontend/src/api/threads.ts
  - frontend/src/components/Sidebar/SearchInput.tsx
  - frontend/src/components/Sidebar/ThreadList.tsx
autonomous: true

must_haves:
  truths:
    - "A search input with a clear button is visible at the top of the sidebar thread list"
    - "Typing instantly filters the displayed thread list to titles that match — no network request, no delay"
    - "When no threads match the typed filter, an empty state message is displayed"
    - "Typing 2+ characters and pausing 300ms triggers a backend FTS5 search returning matching threads with snippet and count badge"
    - "Clicking any search result navigates to that thread and clears the search"
    - "Ctrl+K from anywhere focuses the search input, expanding the sidebar if collapsed"
    - "Active thread is not pinned during filter — it disappears if its title does not match"
  artifacts:
    - path: "frontend/src/hooks/useDebounce.ts"
      provides: "Generic useDebounce hook using useRef + setTimeout"
      exports: ["useDebounce"]
    - path: "frontend/src/api/threads.ts"
      provides: "searchThreads() API function"
      exports: ["searchThreads", "SearchResult"]
    - path: "frontend/src/components/Sidebar/SearchInput.tsx"
      provides: "Controlled search input with FTS5 results display"
      exports: ["SearchInput"]
    - path: "frontend/src/components/Sidebar/ThreadList.tsx"
      provides: "ThreadList wired with search state, title filtering, Ctrl+K handler"
      contains: "SearchInput"
  key_links:
    - from: "frontend/src/components/Sidebar/SearchInput.tsx"
      to: "/api/threads/search"
      via: "searchThreads() called when debouncedQuery >= 2 chars"
      pattern: "searchThreads"
    - from: "frontend/src/components/Sidebar/ThreadList.tsx"
      to: "frontend/src/components/Sidebar/SearchInput.tsx"
      via: "SearchInput rendered above thread groups, query state lifted"
      pattern: "SearchInput"
    - from: "frontend/src/components/Sidebar/ThreadList.tsx"
      to: "document keydown"
      via: "Ctrl+K handler focuses search input ref"
      pattern: "ctrlKey.*[kK]"
---

<objective>
Build the frontend search experience: SearchInput component with Fluent SearchBox, client-side title filter, debounced FTS5 results with snippet and count badge, Ctrl+K shortcut, and empty states.

Purpose: This is the user-facing search feature — without it, users have no way to find threads in the sidebar. Combines instant title filtering with deeper content search.
Output: Working search in the sidebar with two-tier results (title matches + message content matches), keyboard shortcut, and clear-on-navigate behavior.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-thread-search/23-RESEARCH.md
@.planning/phases/23-thread-search/23-01-SUMMARY.md

@frontend/src/components/Sidebar/ThreadList.tsx
@frontend/src/components/Sidebar/ThreadItem.tsx
@frontend/src/api/threads.ts
@frontend/src/contexts/ThreadContext.tsx
@frontend/src/contexts/ChatContext.tsx
@frontend/src/hooks/useStreamingMessage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useDebounce hook and searchThreads API function</name>
  <files>frontend/src/hooks/useDebounce.ts, frontend/src/api/threads.ts</files>
  <action>
1. Create `frontend/src/hooks/useDebounce.ts`:
   - Generic hook: `export function useDebounce<T>(value: T, delay: number): T`
   - Uses useRef<ReturnType<typeof setTimeout> | null> for timer
   - useEffect clears previous timer, sets new one, cleanup clears on unmount
   - Returns debounced value via useState
   - Pattern consistent with rafRef in useStreamingMessage.ts

2. Add to `frontend/src/api/threads.ts`:
   - Export `SearchResult` interface: `{ id: number; name: string; updated_at: string; snippet: string }`
   - Export `searchThreads(q: string): Promise<SearchResult[]>` function
   - Implementation: `fetch(/api/threads/search?q=${encodeURIComponent(q)})`, throw on !res.ok, return res.json()
   - Follow the exact same pattern as the existing `listThreads()`, `createThread()`, etc. in this file
  </action>
  <verify>
    - `cd frontend && npx tsc --noEmit` passes without type errors
    - The useDebounce hook exports correctly: check import resolves
    - searchThreads function signature matches SearchResult interface
  </verify>
  <done>
    - useDebounce hook exists and is importable
    - searchThreads API function exists, typed with SearchResult interface
    - No TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: SearchInput component and ThreadList integration</name>
  <files>frontend/src/components/Sidebar/SearchInput.tsx, frontend/src/components/Sidebar/ThreadList.tsx</files>
  <action>
1. Create `frontend/src/components/Sidebar/SearchInput.tsx`:
   - Props: `{ query: string; onQueryChange: (q: string) => void; ftsResults: SearchResult[]; ftsLoading: boolean; onSelectResult: (threadId: number) => void; inputRef: React.RefObject<HTMLInputElement | null> }`
   - Render Fluent `SearchBox` from `@fluentui/react-components` with:
     - `value={query}`, `onChange` wired to onQueryChange
     - Placeholder text (Claude's discretion, e.g. "Search threads...")
     - `contentAfter` with DismissRegular icon button to clear (or use SearchBox's built-in dismiss)
     - Wire the inputRef to the SearchBox's internal input via `ref` or `input` prop for Ctrl+K focus
   - Below the SearchBox, if ftsLoading, show a small `Spinner` (size="tiny" or "extra-small")
   - Below that, if ftsResults.length > 0 and query.length >= 2:
     - Render a "Message matches" heading with a `CounterBadge` showing ftsResults.length
     - Render each result as a clickable item showing thread name + snippet as subtitle text
     - onClick calls onSelectResult(result.id)
     - Style: use Atlas design tokens (--atlas-*) for consistency. Snippet text should be muted/secondary color, truncated to 1-2 lines
   - If query.length >= 2 and !ftsLoading and ftsResults.length === 0:
     - Show a subtle empty state for FTS (e.g. "No message matches")

2. Modify `frontend/src/components/Sidebar/ThreadList.tsx`:
   - Add state: `const [searchQuery, setSearchQuery] = useState('')`
   - Import and use `useDebounce`: `const debouncedQuery = useDebounce(searchQuery, 300)`
   - Add state for FTS results: `const [ftsResults, setFtsResults] = useState<SearchResult[]>([])`
   - Add state for FTS loading: `const [ftsLoading, setFtsLoading] = useState(false)`
   - Add a ref for the search input: `const searchInputRef = useRef<HTMLInputElement>(null)`
   - Add useEffect for FTS5 fetch: when debouncedQuery.trim().length >= 2, call searchThreads(debouncedQuery), set results. Use cancelled flag pattern for cleanup. When < 2 chars, clear ftsResults.
   - Add client-side title filter: `const filteredThreads = searchQuery.trim() ? threads.filter(t => (t.name || 'New Chat').toLowerCase().includes(searchQuery.trim().toLowerCase())) : threads;`
   - Use filteredThreads instead of threads for groupThreadsByRecency and rendering
   - When filteredThreads is empty AND searchQuery is non-empty, show an empty state message (e.g. "No threads match" or similar)
   - Render `<SearchInput>` above the thread groups inside the `{!collapsed && ...}` block
   - Wire onSelectResult: call the existing thread selection handler (handleSelectThread or equivalent), then clear searchQuery
   - Add Ctrl+K handler via useEffect:
     - Listen for keydown on document
     - If (e.ctrlKey || e.metaKey) && e.key === 'k': preventDefault, if collapsed call onToggleCollapse() to expand sidebar, then focus searchInputRef.current (use setTimeout(0) if expanding to defer focus until after DOM update)
     - Cleanup: remove event listener
   - Pass searchInputRef to SearchInput component

   IMPORTANT:
   - Do NOT pin the active thread during filtering — if its title doesn't match, it disappears from the list
   - Clear search query when navigating to a result (user found what they wanted)
   - The Ctrl+K handler must expand collapsed sidebar before focusing
   - Use Fluent SearchBox (not a custom input) — it provides the dismiss button and search icon per SRCH-01
  </action>
  <verify>
    - `cd frontend && npx tsc --noEmit` passes
    - `cd frontend && npm run build` succeeds (produces dist/)
    - Visual check: search input visible at top of sidebar thread list
    - Typing filters thread titles instantly
    - Typing 2+ chars shows FTS results after 300ms pause
    - Clicking a result navigates to thread and clears search
    - Ctrl+K focuses the search input
    - Empty state shown when no threads match
  </verify>
  <done>
    - SearchInput component renders Fluent SearchBox with dismiss button at top of sidebar
    - Client-side title filter works instantly on keystroke
    - FTS5 results appear in "Message matches" section with CounterBadge after 300ms debounce / 2-char minimum
    - Clicking a result navigates to the thread and clears the search state
    - Ctrl+K focuses the search input from anywhere, expanding sidebar if collapsed
    - Empty state displayed when no threads match the filter
    - Active thread is not pinned during filter
    - Frontend builds without errors
  </done>
</task>

</tasks>

<verification>
- Frontend TypeScript compiles without errors
- Frontend builds to dist/ without errors
- Search input visible at top of sidebar with clear button
- Title filtering is instant and client-side only
- FTS5 search triggers after 300ms with 2+ chars
- FTS5 results show thread name + snippet + count badge
- Clicking result navigates and clears search
- Ctrl+K focuses search from anywhere
- Empty state when no matches
- Active thread not pinned during filter
</verification>

<success_criteria>
- SRCH-01: Search input with clear button at top of sidebar thread list
- SRCH-02: Instant client-side title filter that does not affect active thread
- SRCH-03: Empty state when no threads match
- SRCH-04: FTS5 backend search via /api/threads/search
- SRCH-05: Results show thread name + snippet with count badge
- SRCH-06: Click result navigates to thread
- SRCH-07: 300ms debounce, 2-char minimum for FTS5
- SRCH-08: Ctrl+K focuses search input
</success_criteria>

<output>
After completion, create `.planning/phases/23-thread-search/23-02-SUMMARY.md`
</output>
