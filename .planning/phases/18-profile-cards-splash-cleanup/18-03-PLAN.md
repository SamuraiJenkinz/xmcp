---
phase: 18-profile-cards-splash-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_exchange_client.py
  - tests/test_server.py
  - chat_app/graph_client.py
  - tests/test_graph_client.py
  - exchange_mcp/tools.py
autonomous: true

must_haves:
  truths:
    - "test_build_cmdlet_script_interactive passes"
    - "test_build_cmdlet_script_cba passes"
    - "test_server.py module docstring says 17 tools, not 15"
    - "get_user_photo_bytes function no longer exists in graph_client.py"
    - "No tests reference get_user_photo_bytes"
    - "get_colleague_profile user_id schema description mentions Graph API object ID (GUID)"
  artifacts:
    - path: "tests/test_exchange_client.py"
      provides: "Fixed test assertions matching current _build_cmdlet_script output"
    - path: "tests/test_server.py"
      provides: "Corrected module docstring tool count"
      contains: "17"
    - path: "chat_app/graph_client.py"
      provides: "graph_client without dead get_user_photo_bytes function"
    - path: "tests/test_graph_client.py"
      provides: "Test file without get_user_photo_bytes test functions"
    - path: "exchange_mcp/tools.py"
      provides: "Corrected user_id schema description"
      contains: "Graph API object ID"
  key_links:
    - from: "tests/test_exchange_client.py"
      to: "exchange_mcp/exchange_client.py"
      via: "_build_cmdlet_script assertions"
      pattern: "_build_cmdlet_script"
    - from: "tests/test_graph_client.py"
      to: "chat_app/graph_client.py"
      via: "import chat_app.graph_client"
      pattern: "import chat_app.graph_client"
---

<objective>
Fix 3 test regressions (DEBT-03), remove get_user_photo_bytes() dead code (DEBT-04), and fix get_colleague_profile user_id schema description (DEBT-05).

Purpose: Three categories of tech debt accumulated across v1.0 and v1.1. The test regressions are from implementation changes that weren't reflected in test assertions. The dead code is an orphaned function replaced by get_user_photo_96(). The schema description is vague about what user_id actually is. All three are mechanical fixes.

Output: Passing test suite for the 3 regression tests, cleaned graph_client.py, corrected schema.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-profile-cards-splash-cleanup/18-CONTEXT.md
@.planning/phases/18-profile-cards-splash-cleanup/18-RESEARCH.md
@tests/test_exchange_client.py
@tests/test_server.py
@tests/test_graph_client.py
@chat_app/graph_client.py
@exchange_mcp/tools.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 3 test regressions (DEBT-03)</name>
  <files>tests/test_exchange_client.py, tests/test_server.py</files>
  <action>
  **Fix 1: test_build_cmdlet_script_interactive** (line ~124 in test_exchange_client.py)

  The test expects `"Disconnect-ExchangeOnline"` in the script output, but the implementation changed: the finally block now contains a comment saying "Session ends when process exits -- no explicit disconnect needed" instead of calling Disconnect-ExchangeOnline. The test assertion on line 137 must be updated.

  Replace:
  ```python
  assert "Disconnect-ExchangeOnline" in script, "Missing Disconnect-ExchangeOnline"
  ```
  With:
  ```python
  assert "finally" in script, "Missing finally block"
  ```
  Wait — the test already has `assert "finally" in script` on line 138. So just REMOVE the `assert "Disconnect-ExchangeOnline" in script` line (line 137). The finally block check is already covered.

  **Fix 2: test_build_cmdlet_script_cba** (line ~149 in test_exchange_client.py)

  Two assertion failures:
  1. Line 157: `assert "$env:AZURE_CERT_THUMBPRINT" in script` — the implementation now inlines the actual value ('AABBCCDDEEFF...') instead of reading from env var. Replace with assertion that `CertificateThumbPrint` is present (already checked on line 156). Remove line 157.
  2. Line 158: `assert "$env:AZURE_CLIENT_ID" in script` — same issue, value inlined. Remove line 158.
  3. Line 159: `assert "$env:AZURE_TENANT_DOMAIN" in script` — same issue. Remove line 159.
  4. Same as Fix 1: remove `assert "Disconnect-ExchangeOnline" in script` (line 165).

  After removing the 4 lines from test_build_cmdlet_script_cba, the remaining assertions (Connect-ExchangeOnline present, CertificateThumbPrint present, ConvertTo-Json, finally block, cmdlet present) still verify the essential contract.

  **Fix 3: test_server.py module docstring** (line 1-17)

  Update the module docstring: change "all 15 registered tools (14 Exchange + ping)" to "all 17 registered tools (16 Exchange + ping)" and "all 15 tool definitions" to "all 17 tool definitions". These are on lines 5 and 12.

  IMPORTANT: Only fix these 3 specific regressions. Do NOT touch test_tools_flow.py, test_tools_hybrid.py, or test_integration.py — those are pre-existing failures outside DEBT-03 scope.
  </action>
  <verify>
  - `python -m pytest tests/test_exchange_client.py::test_build_cmdlet_script_interactive tests/test_exchange_client.py::test_build_cmdlet_script_cba -v` — both pass
  - `python -m pytest tests/test_server.py -v` — all pass
  - Grep for `Disconnect-ExchangeOnline` in test_exchange_client.py — should return zero matches
  - Grep for `\$env:AZURE_CERT_THUMBPRINT` in test_exchange_client.py — should return zero matches
  </verify>
  <done>
  - test_build_cmdlet_script_interactive passes (removed stale Disconnect assertion)
  - test_build_cmdlet_script_cba passes (removed stale env var and Disconnect assertions)
  - test_server.py docstring says "17" tools, not "15"
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove dead code and fix schema description (DEBT-04, DEBT-05)</name>
  <files>chat_app/graph_client.py, tests/test_graph_client.py, exchange_mcp/tools.py</files>
  <action>
  **DEBT-04: Remove get_user_photo_bytes() dead code**

  1. In `chat_app/graph_client.py`, delete the entire `get_user_photo_bytes()` function (lines 274-305). This function is never called — `get_user_photo_96()` replaced it.

  2. In `tests/test_graph_client.py`, remove these 4 test functions and their associated comments/markers:
     - `test_get_user_photo_bytes_returns_none_when_graph_disabled` (line ~97)
     - `test_get_user_photo_bytes_empty_user_id_returns_none` (line ~131)
     - `test_get_user_photo_bytes_returns_none_on_404` (line ~194)
     - `test_get_user_photo_bytes_returns_bytes_on_200` (line ~203)
     Also remove the section header comment "# get_user_photo_bytes tests" (line ~190) if present.

  3. Update the `test_graph_client.py` module docstring: remove the 4 bullet points that reference get_user_photo_bytes. The docstring lists test descriptions in lines 8-15.

  **DEBT-05: Fix schema description**

  In `exchange_mcp/tools.py`, find the `get_colleague_profile` tool definition (around line 414). Change the user_id description from:
  ```python
  "description": "The colleague's user ID from search results",
  ```
  To:
  ```python
  "description": "The colleague's Microsoft Graph API object ID (GUID) from search_colleagues results",
  ```
  </action>
  <verify>
  - `python -c "from chat_app.graph_client import get_user_photo_bytes"` should fail with ImportError
  - `python -c "from chat_app.graph_client import get_user_photo_96; print('OK')"` should succeed
  - `python -m pytest tests/test_graph_client.py -v` — all remaining tests pass, no get_user_photo_bytes tests exist
  - Grep for `get_user_photo_bytes` in chat_app/graph_client.py — zero matches
  - Grep for `get_user_photo_bytes` in tests/test_graph_client.py — zero matches
  - Grep for `Graph API object ID` in exchange_mcp/tools.py — one match in get_colleague_profile schema
  </verify>
  <done>
  - get_user_photo_bytes() removed from graph_client.py
  - All 4 tests for get_user_photo_bytes removed from test_graph_client.py
  - test_graph_client.py docstring updated
  - get_colleague_profile user_id description clarified as "Microsoft Graph API object ID (GUID)"
  - All remaining graph_client tests pass
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_exchange_client.py::test_build_cmdlet_script_interactive tests/test_exchange_client.py::test_build_cmdlet_script_cba tests/test_server.py tests/test_graph_client.py -v` — all pass
- No references to get_user_photo_bytes in chat_app/graph_client.py or tests/test_graph_client.py
- get_colleague_profile user_id description contains "Graph API object ID"
- test_server.py docstring says "17" tools
</verification>

<success_criteria>
1. test_build_cmdlet_script_interactive and test_build_cmdlet_script_cba both pass
2. test_server.py docstring accurately reflects 17 tools
3. get_user_photo_bytes() function and its 4 tests are completely removed
4. get_colleague_profile user_id schema description says "Microsoft Graph API object ID (GUID)"
5. All tests in test_graph_client.py and test_server.py pass
</success_criteria>

<output>
After completion, create `.planning/phases/18-profile-cards-splash-cleanup/18-03-SUMMARY.md`
</output>
