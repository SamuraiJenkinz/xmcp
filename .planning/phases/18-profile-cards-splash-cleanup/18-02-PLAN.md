---
phase: 18-profile-cards-splash-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - chat_app/templates/splash.html
  - chat_app/static/style.css
autonomous: true

must_haves:
  truths:
    - "Splash page shows Atlas wordmark or geometric logo mark using --atlas-accent color, not a lightning emoji"
    - "Sign-in card uses elevated surface background with 12px border-radius and --atlas-stroke-1 border, no box-shadow"
    - "Atlas heading is 28px semibold, subtitle is 14px secondary color, description is concise"
    - "Sign in with Microsoft button uses --atlas-accent background with white text and 8px border-radius"
    - "Dark mode renders correctly (--color-* tokens in style.css map to atlas-equivalent values)"
    - "Page is full viewport height centered with canvas background"
  artifacts:
    - path: "chat_app/templates/splash.html"
      provides: "Fluent 2 splash page markup"
      contains: "splash-card"
    - path: "chat_app/static/style.css"
      provides: "Updated splash CSS with atlas-equivalent tokens"
      contains: "splash-container"
  key_links:
    - from: "chat_app/templates/splash.html"
      to: "chat_app/static/style.css"
      via: "CSS class references"
      pattern: "splash-card"
---

<objective>
Redesign the login/splash page to a professional Fluent 2 landing appearance.

Purpose: SPLA-01 requires the splash page to look professional, not a bare form. The current page has a lightning emoji, box-shadow on the card, and border-radius: 16px. This plan updates the markup and CSS to use Fluent 2 aesthetics: atlas-equivalent color tokens in style.css, 12px border-radius, border instead of shadow, accent-colored sign-in button, and proper typography sizing.

Output: Updated splash.html template and style.css splash section.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-profile-cards-splash-cleanup/18-CONTEXT.md
@.planning/phases/18-profile-cards-splash-cleanup/18-RESEARCH.md
@chat_app/templates/splash.html
@chat_app/static/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update splash.html markup for Fluent 2 aesthetic</name>
  <files>chat_app/templates/splash.html</files>
  <action>
  Replace the splash page content block with updated markup:

  1. Replace the lightning emoji (`&#9889;`) with a simple geometric logo mark — an SVG circle or diamond shape using the accent color. Use an inline SVG: a 40px circle with a stylized "A" letter or a simple geometric shape (e.g., a rounded square rotated 45 degrees). Keep it simple and professional. Apply class `splash-logo` instead of `splash-icon`.

  2. Keep the heading as "Atlas" with an `<h1>` tag.

  3. Keep the subtitle as "MMC Exchange Infrastructure Assistant" with class `splash-subtitle`.

  4. Simplify the description to a single concise sentence, e.g.: "Conversational queries against live Exchange infrastructure data." Apply class `splash-description`.

  5. Keep the Microsoft sign-in button with the Microsoft logo SVG intact. Keep the `url_for('auth.login')` href.

  The overall structure remains: splash-container > splash-card > logo + h1 + subtitle + description + button.
  </action>
  <verify>
  - The template extends base.html (Jinja2 inheritance preserved)
  - No `&#9889;` lightning emoji remains
  - The `url_for('auth.login')` link is preserved
  - Microsoft logo SVG is preserved inside the sign-in button
  </verify>
  <done>
  - Lightning emoji replaced with geometric logo mark SVG
  - Description condensed to one sentence
  - Template structure and Jinja2 inheritance intact
  </done>
</task>

<task type="auto">
  <name>Task 2: Update splash CSS for Fluent 2 tokens and geometry</name>
  <files>chat_app/static/style.css</files>
  <action>
  The splash page uses `--color-*` tokens defined in style.css `:root` and `[data-theme="dark"]` blocks. Do NOT replace the --color-* namespace with --atlas-* (the Flask app uses its own token system). Instead, update the splash-specific CSS rules to match Fluent 2 geometry:

  Update `.splash-container`:
  - Ensure `min-height: 100vh` and centering with flexbox
  - Background: `var(--color-bg-page)` (already correct — this is the canvas equivalent)

  Update `.splash-card`:
  - Change `border-radius` from `16px` to `12px`
  - Remove any `box-shadow` if present
  - Add `border: 1px solid var(--color-border)` if not already present
  - Background: `var(--color-bg-surface)` (already correct — this is the elevated equivalent)
  - Padding: `40px` for comfortable spacing

  Add `.splash-logo` rule (replaces `.splash-icon`):
  - Center the SVG
  - `margin-bottom: 16px`

  Remove `.splash-icon` rule entirely (replaced by `.splash-logo`).

  Update `.splash-card h1`:
  - `font-size: 28px`
  - `font-weight: 600`
  - `font-family: 'Segoe UI Variable', 'Segoe UI', system-ui, sans-serif`

  Update `.splash-subtitle`:
  - `font-size: 14px`
  - `color: var(--color-text-muted)`

  Update `.splash-description`:
  - `font-size: 14px`
  - `color: var(--color-text-secondary)`
  - `max-width: 320px` for comfortable reading width
  - `text-align: center`

  Update `.btn-signin`:
  - Background: `var(--color-brand)` (already correct)
  - Color: `var(--color-on-brand)` (already correct)
  - `border-radius: 8px` (if different)
  - `font-weight: 600`
  - `font-size: 14px`
  - Ensure no box-shadow on the button

  Update `.btn-signin:hover`:
  - Background: `var(--color-brand-hover)` (already correct)
  </action>
  <verify>
  - Grep for `splash-icon` in style.css — should return zero matches (replaced by splash-logo)
  - Grep for `border-radius: 12px` in the `.splash-card` rule — should exist
  - Grep for `box-shadow` in splash-related rules — should return zero matches
  - Run the Flask app briefly or visually inspect splash.html renders with correct styling
  </verify>
  <done>
  - Splash card has 12px border-radius, border instead of shadow, 40px padding
  - Heading is 28px semibold Segoe UI Variable
  - Button has 8px border-radius, brand color background
  - splash-icon replaced by splash-logo
  - Dark mode tokens continue to work (--color-* namespace preserved)
  </done>
</task>

</tasks>

<verification>
- splash.html loads without Jinja2 template errors (extends base.html, url_for works)
- No lightning emoji in the markup
- No box-shadow in splash CSS rules
- border-radius: 12px on splash card
- Dark mode class [data-theme="dark"] still overrides all --color-* tokens used by splash
</verification>

<success_criteria>
1. Splash page has a professional Fluent 2 appearance — geometric logo, not emoji
2. Card uses border not shadow, 12px radius, proper typography sizes
3. Sign-in button uses accent color with 8px radius
4. Dark mode works correctly (--color-* tokens override in [data-theme="dark"])
5. All Jinja2 template inheritance and auth links preserved
</success_criteria>

<output>
After completion, create `.planning/phases/18-profile-cards-splash-cleanup/18-02-SUMMARY.md`
</output>
