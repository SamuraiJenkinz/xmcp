---
phase: 26-message-trace-tool
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - chat_app/openai_client.py
  - exchange_mcp/server.py
autonomous: true

must_haves:
  truths:
    - "System prompt contains disambiguation section for get_message_trace vs check_mail_flow"
    - "System prompt includes explicit negative guidance: do NOT use check_mail_flow for delivery tracking"
    - "System prompt includes example queries for get_message_trace"
    - "System prompt includes clarification prompt for ambiguous requests"
    - "Server docstring reflects 18 tools"
  artifacts:
    - path: "chat_app/openai_client.py"
      provides: "Message Trace vs Mail Flow disambiguation in SYSTEM_PROMPT"
      contains: "Message Trace vs Mail Flow"
    - path: "exchange_mcp/server.py"
      provides: "Updated tool count in docstring"
      contains: "18 tools"
  key_links:
    - from: "SYSTEM_PROMPT in openai_client.py"
      to: "get_message_trace tool"
      via: "Intent-based disambiguation rules"
      pattern: "get_message_trace.*delivery.*status"
    - from: "SYSTEM_PROMPT in openai_client.py"
      to: "check_mail_flow tool"
      via: "Negative guidance rule"
      pattern: "Do NOT use check_mail_flow.*delivery"
---

<objective>
Add system prompt disambiguation between get_message_trace and check_mail_flow tools, and update the server tool count. This ensures the AI correctly routes delivery tracking questions to get_message_trace and routing topology questions to check_mail_flow.

Purpose: Without explicit disambiguation, the AI will confuse the two tools — both involve "email" and "flow." Intent-based rules with negative guidance prevent the most likely misrouting.
Output: Updated SYSTEM_PROMPT in openai_client.py with disambiguation section; updated docstring in server.py.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-message-trace-tool/26-RESEARCH.md
@.planning/phases/26-message-trace-tool/26-CONTEXT.md
@.planning/phases/26-message-trace-tool/26-01-SUMMARY.md
@chat_app/openai_client.py
@exchange_mcp/server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add system prompt disambiguation section</name>
  <files>chat_app/openai_client.py</files>
  <action>
    Add a new section to SYSTEM_PROMPT in chat_app/openai_client.py. Place it after the existing "## Connector Queries" section (which ends around line 68). Follow the existing section pattern (## heading, descriptive text, numbered rules).

    Add this section (adapt rule numbers to follow the last existing rule number):

    ```
    ## Message Trace vs Mail Flow

    You have two different tools for email-related questions:
    - **get_message_trace**: Tracks actual delivery status of specific messages already sent via Get-MessageTraceV2.
      Use when: "Did my email arrive?", "Was the invoice delivered to bob@contoso.com?", "Show emails from alice@contoso.com in the last hour", "Why did bob's email fail?", "Trace emails from john@example.com in the last 3 days".
    - **check_mail_flow**: Tests whether email CAN flow between two addresses (routing topology and connector configuration).
      Use when: "Can Alice email Bob?", "Is there a connector for fabrikam.com?", "Why is mail from sales blocked to partner@external.com?", "What's the mail route to external.com?"

    [N]. When a user asks about a specific email's delivery status — whether it arrived, was delivered, failed, or is pending — use get_message_trace.
    [N+1]. When a user asks about routing configuration, connectors, or whether mail CAN flow between addresses, use check_mail_flow.
    [N+2]. Do NOT use check_mail_flow when the user asks about whether a specific email arrived or was delivered. That is a get_message_trace question.
    [N+3]. If unsure whether the user wants delivery tracking or routing analysis, ask: "Are you asking about a specific email that was already sent (delivery status), or about mail routing configuration?"
    ```

    Replace [N], [N+1], [N+2], [N+3] with the correct sequential rule numbers following the last existing rule number in SYSTEM_PROMPT. Read the file first to determine the current last rule number.
  </action>
  <verify>
    1. `python -c "from chat_app.openai_client import SYSTEM_PROMPT; assert 'Message Trace vs Mail Flow' in SYSTEM_PROMPT; assert 'get_message_trace' in SYSTEM_PROMPT; assert 'Do NOT use check_mail_flow' in SYSTEM_PROMPT; print('prompt ok')"` succeeds
    2. `python -m py_compile chat_app/openai_client.py` succeeds
  </verify>
  <done>
    - SYSTEM_PROMPT contains "## Message Trace vs Mail Flow" section
    - Section includes example queries for both tools
    - Section includes explicit negative guidance rule
    - Section includes clarification prompt for ambiguous requests
    - Rule numbers are sequential with existing rules
  </done>
</task>

<task type="auto">
  <name>Task 2: Update server.py tool count and verify end-to-end</name>
  <files>exchange_mcp/server.py</files>
  <action>
    1. Update the docstring in exchange_mcp/server.py that says "17 tools" to say "18 tools". Also update the breakdown count (currently "14 Exchange + ping + 2 Graph colleague tools" should become "15 Exchange + ping + 2 Graph colleague tools").

    2. Run a comprehensive verification:
       - Confirm TOOL_DEFINITIONS has 18 entries
       - Confirm TOOL_DISPATCH has 18 entries
       - Confirm get_message_trace is in both
       - Confirm SYSTEM_PROMPT contains disambiguation
       - Compile all modified files
  </action>
  <verify>
    1. `python -c "from exchange_mcp.tools import TOOL_DEFINITIONS, TOOL_DISPATCH; assert len(TOOL_DEFINITIONS) == 18, f'Expected 18 defs, got {len(TOOL_DEFINITIONS)}'; assert len(TOOL_DISPATCH) == 18, f'Expected 18 dispatch, got {len(TOOL_DISPATCH)}'; print(f'tools: {len(TOOL_DEFINITIONS)} defs, {len(TOOL_DISPATCH)} dispatch')"` succeeds
    2. `python -m py_compile exchange_mcp/server.py` succeeds
    3. `grep -c "18 tools" exchange_mcp/server.py` returns 1
  </verify>
  <done>
    - server.py docstring says "18 tools" and "15 Exchange + ping + 2 Graph colleague tools"
    - TOOL_DEFINITIONS count is 18
    - TOOL_DISPATCH count is 18
    - All modified files compile cleanly
  </done>
</task>

</tasks>

<verification>
- `python -m py_compile chat_app/openai_client.py` passes
- `python -m py_compile exchange_mcp/server.py` passes
- SYSTEM_PROMPT contains "Message Trace vs Mail Flow" disambiguation section
- SYSTEM_PROMPT contains negative guidance: "Do NOT use check_mail_flow" for delivery questions
- server.py docstring says "18 tools"
- TOOL_DEFINITIONS and TOOL_DISPATCH both have 18 entries
</verification>

<success_criteria>
- AI can correctly distinguish between get_message_trace (delivery tracking) and check_mail_flow (routing topology) based on system prompt guidance
- System prompt includes example queries, intent-based rules, explicit negative guidance, and ambiguity clarification prompt
- Tool count is accurate across all references
- All files compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/26-message-trace-tool/26-02-SUMMARY.md`
</output>
