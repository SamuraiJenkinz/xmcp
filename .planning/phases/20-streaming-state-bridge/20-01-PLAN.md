---
phase: 20-streaming-state-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/vite.config.ts
  - frontend/src/components/AppLayout.tsx
autonomous: true

must_haves:
  truths:
    - "During streaming, InputArea receives isStreaming=true and renders Stop button instead of Send"
    - "ThreadList reads isStreaming=true from ChatContext during an active stream"
    - "npm run dev proxies POST /chat/stream to Flask backend without 404"
    - "Pressing Stop button during streaming cancels the stream and reverts to Send"
    - "Pressing Escape during streaming cancels the stream"
    - "Switching threads during streaming aborts the active stream"
  artifacts:
    - path: "frontend/vite.config.ts"
      provides: "/chat proxy entry"
      contains: "'/chat'"
    - path: "frontend/src/components/AppLayout.tsx"
      provides: "useEffect bridge from hook isStreaming to context SET_STREAMING"
      contains: "SET_STREAMING"
  key_links:
    - from: "useStreamingMessage hook isStreaming state"
      to: "ChatContext isStreaming"
      via: "useEffect dispatching SET_STREAMING in AppLayout"
      pattern: "dispatch.*SET_STREAMING.*hookIsStreaming"
    - from: "frontend fetch /chat/stream"
      to: "Flask backend"
      via: "Vite dev proxy"
      pattern: "'/chat'.*target.*127\\.0\\.0\\.1:5000"
---

<objective>
Bridge the isStreaming state from the useStreamingMessage hook to ChatContext so all consuming components (InputArea, ThreadList) receive the correct streaming state, and add the missing /chat proxy to vite.config.ts so SSE streaming works in dev mode.

Purpose: These are the two root causes identified in the v1.2 milestone audit that prevent Stop button rendering, Escape cancel, and thread-switch abort from functioning.
Output: Two modified files that close both CHAT-03 and FRAME-08 gaps.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/vite.config.ts
@frontend/src/components/AppLayout.tsx
@frontend/src/contexts/ChatContext.tsx
@frontend/src/hooks/useStreamingMessage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /chat proxy to Vite dev server config</name>
  <files>frontend/vite.config.ts</files>
  <action>
Add a `/chat` proxy entry to the `server.proxy` object in `vite.config.ts`:

```typescript
'/chat': { target: 'http://127.0.0.1:5000', changeOrigin: true },
```

Place it after the `/api` entry for logical grouping. No SSE-specific options are needed — Vite's proxy handles streaming responses correctly by default.

Do NOT add `/chat/stream` as a separate entry — the `/chat` prefix covers all sub-paths including `/chat/stream`.
  </action>
  <verify>
Read vite.config.ts and confirm the `/chat` proxy entry exists with target `http://127.0.0.1:5000` and changeOrigin: true.
Run `npx tsc --noEmit` from frontend/ to confirm no type errors.
  </verify>
  <done>vite.config.ts contains '/chat' proxy entry targeting Flask backend</done>
</task>

<task type="auto">
  <name>Task 2: Bridge hook isStreaming to ChatContext via useEffect</name>
  <files>frontend/src/components/AppLayout.tsx</files>
  <action>
In AppLayout.tsx, make these changes:

1. Add `useEffect` to the import from 'react' (line 1 currently imports useCallback and useState).

2. Destructure `isStreaming` from the `useStreamingMessage` return value, aliased to avoid collision:
   Change line 21 from:
   `const { startStream, cancelStream } = useStreamingMessage({`
   to:
   `const { startStream, cancelStream, isStreaming: hookIsStreaming } = useStreamingMessage({`

3. Add a useEffect immediately after the useStreamingMessage call (after line 53, before handleSend) that bridges hook state to context:
   ```typescript
   useEffect(() => {
     chatDispatch({ type: 'SET_STREAMING', isStreaming: hookIsStreaming });
   }, [hookIsStreaming, chatDispatch]);
   ```

4. Do NOT change the JSX. InputArea already receives `isStreaming` from context (line 109). ThreadList already reads `isStreaming` from context (ThreadList.tsx line 18). The useEffect bridge makes context the single source of truth for consumers, fed by the hook.

5. Do NOT remove SET_STREAMING from the ChatAction union or the reducer — it is now actively used by this bridge.

6. Do NOT add any conditional guard to the useEffect (e.g., `if (hookIsStreaming !== isStreaming)`). The dispatch is idempotent and the one-render lag is acceptable (<16ms).
  </action>
  <verify>
Run `npx tsc --noEmit` from frontend/ to confirm no type errors.
Run `npx eslint src/components/AppLayout.tsx` from frontend/ to confirm no lint errors.
Read AppLayout.tsx and verify:
  - useEffect is imported
  - hookIsStreaming is destructured from useStreamingMessage return
  - useEffect dispatches SET_STREAMING with hookIsStreaming
  - dependency array contains both hookIsStreaming and chatDispatch
  - JSX is unchanged — InputArea still receives isStreaming from context
  </verify>
  <done>
  AppLayout bridges useStreamingMessage's isStreaming to ChatContext via useEffect dispatch of SET_STREAMING. All downstream components (InputArea, ThreadList) now receive true isStreaming values during active streams.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors from frontend/
2. `npm run build` succeeds from frontend/
3. Read vite.config.ts — '/chat' proxy entry present
4. Read AppLayout.tsx — useEffect bridge present, hookIsStreaming destructured, SET_STREAMING dispatched
5. Grep for `SET_STREAMING` in ChatContext.tsx — action type and reducer case still present
6. Grep for `isStreaming` in ThreadList.tsx — still reads from useChat() context (line 18)
</verification>

<success_criteria>
- vite.config.ts has /chat proxy entry
- AppLayout.tsx imports useEffect and bridges hookIsStreaming to context via SET_STREAMING dispatch
- TypeScript compilation passes
- Build succeeds
- No changes to ChatContext.tsx, ThreadList.tsx, InputArea.tsx, or useStreamingMessage.ts
</success_criteria>

<output>
After completion, create `.planning/phases/20-streaming-state-bridge/20-01-SUMMARY.md`
</output>
