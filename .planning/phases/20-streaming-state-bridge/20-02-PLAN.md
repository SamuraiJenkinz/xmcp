---
phase: 20-streaming-state-bridge
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "SET_STREAMING reducer case is actively used — confirmed by grep showing dispatch in AppLayout"
    - "No dead SET_STREAMING comments or TODO markers remain in codebase"
    - "npm run build produces zero errors and zero warnings"
    - "All five Phase 20 success criteria from ROADMAP.md are verifiably met"
  artifacts: []
  key_links:
    - from: "AppLayout useEffect"
      to: "ChatContext SET_STREAMING reducer case"
      via: "dispatch call"
      pattern: "SET_STREAMING"
---

<objective>
Verify the complete streaming state bridge works end-to-end: confirm no dead code remains, build passes cleanly, and all five ROADMAP success criteria are met. This plan is a verification-only checkpoint with no file modifications expected.

Purpose: Phase 20 closes the final v1.2 milestone gaps. This plan confirms closure before marking the phase complete.
Output: Verified phase with human sign-off on streaming UX behavior.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-streaming-state-bridge/20-01-SUMMARY.md
@frontend/src/components/AppLayout.tsx
@frontend/src/contexts/ChatContext.tsx
@frontend/src/hooks/useStreamingMessage.ts
@frontend/vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dead code audit and build verification</name>
  <files></files>
  <action>
Run these verification checks from the frontend/ directory:

1. **Grep for SET_STREAMING usage:** Search all .ts and .tsx files for "SET_STREAMING". Confirm it appears in:
   - ChatContext.tsx (action type union + reducer case)
   - AppLayout.tsx (useEffect dispatch)
   There should be NO other files dispatching SET_STREAMING. If any TODO/FIXME/dead-code comments mentioning SET_STREAMING exist, note them.

2. **Grep for stale isStreaming comments:** Search for any comments containing "dead code", "unused", "TODO", or "FIXME" near isStreaming or SET_STREAMING references. These would be leftover from pre-bridge state.

3. **Build verification:** Run `npm run build` from frontend/ and confirm zero errors and zero warnings.

4. **TypeScript verification:** Run `npx tsc --noEmit` from frontend/ and confirm zero errors.

5. **Trace the full isStreaming data flow** by reading key lines:
   - useStreamingMessage.ts: `setIsStreaming(true)` called in startStream
   - AppLayout.tsx: `hookIsStreaming` destructured, useEffect dispatches SET_STREAMING
   - ChatContext.tsx: SET_STREAMING reducer sets `isStreaming` on state
   - AppLayout.tsx JSX: `isStreaming={isStreaming}` passed to InputArea (from context)
   - ThreadList.tsx: `const { isStreaming } = useChat()` reads from context
   - InputArea.tsx: `isStreaming` prop controls Stop button render and Escape handler

If any dead code comments about SET_STREAMING are found, remove them and re-run build. Otherwise, no file modifications needed.
  </action>
  <verify>
All greps return expected results. Build and tsc pass. Data flow trace confirms hook -> useEffect -> context -> consumers chain is complete.
  </verify>
  <done>SET_STREAMING is actively used with no dead code markers. Build passes. isStreaming data flow is fully traced from hook through context to all consumers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Complete streaming state bridge: useStreamingMessage hook's isStreaming is now bridged to ChatContext via useEffect in AppLayout. Vite dev proxy covers /chat/stream. All downstream components (InputArea, ThreadList) receive correct streaming state.
  </what-built>
  <how-to-verify>
  Start the dev environment (`npm run dev` in frontend/, Flask backend running on :5000).

  **Test 1 — Stop button renders during streaming:**
  1. Open http://localhost:5173 in browser
  2. Send a message that triggers a long response
  3. Verify the Send button is replaced by a Stop button during streaming
  4. Press Stop — verify stream cancels and button reverts to Send

  **Test 2 — Escape cancels streaming:**
  1. Send another message
  2. While streaming, press Escape
  3. Verify stream cancels and "[response cancelled]" appears at end of response

  **Test 3 — Thread switch aborts stream:**
  1. Create a second thread (or have one existing)
  2. Send a message in the current thread
  3. While streaming, click the other thread in the sidebar
  4. Verify the stream aborts and the new thread loads cleanly

  **Test 4 — Vite proxy works:**
  1. Open browser DevTools Network tab
  2. Send a message
  3. Verify the POST to /chat/stream returns 200 (not 404)
  4. Verify SSE events stream in the response

  **Test 5 — No regression:**
  1. Send a normal message and let it complete
  2. Verify the response renders fully with correct formatting
  3. Verify Send button returns to normal after completion
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tests pass, or describe which tests failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
1. Grep confirms SET_STREAMING used in exactly ChatContext.tsx and AppLayout.tsx
2. No dead code comments about SET_STREAMING remain
3. `npm run build` zero errors, zero warnings
4. `npx tsc --noEmit` zero errors
5. Human verified all 5 streaming behaviors work correctly
</verification>

<success_criteria>
- Build passes cleanly
- No dead SET_STREAMING code or stale comments
- Human confirms: Stop button renders during streaming
- Human confirms: Escape cancels streaming
- Human confirms: Thread switch aborts stream
- Human confirms: /chat/stream returns 200 in dev mode
- Human confirms: Normal message flow works without regression
</success_criteria>

<output>
After completion, create `.planning/phases/20-streaming-state-bridge/20-02-SUMMARY.md`
</output>
