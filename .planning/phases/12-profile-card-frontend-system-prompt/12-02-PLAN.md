---
phase: 12-profile-card-frontend-system-prompt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - chat_app/openai_client.py
autonomous: true

must_haves:
  truths:
    - "Atlas uses search_colleagues for name-based queries (e.g., 'find Jane Smith', 'who is Bob?', 'look up alice@company.com')"
    - "Atlas uses get_colleague_profile when it has a specific email or user ID"
    - "When search_colleagues returns exactly 1 match, Atlas auto-calls get_colleague_profile without asking the user"
    - "When search_colleagues returns multiple matches, Atlas lists them and asks the user to pick"
    - "After get_colleague_profile succeeds, Atlas does NOT duplicate the card fields in its text response"
    - "Atlas still handles Exchange infrastructure queries as before (rules 1-6 unchanged)"
  artifacts:
    - path: "chat_app/openai_client.py"
      provides: "Updated SYSTEM_PROMPT with Colleague Lookup section"
      contains: "Colleague Lookup"
  key_links:
    - from: "chat_app/openai_client.py"
      to: "SYSTEM_PROMPT"
      via: "Colleague Lookup section with rules 7-10"
      pattern: "search_colleagues.*get_colleague_profile"
---

<objective>
Update the Atlas system prompt to include colleague lookup tool selection guidance so Atlas reliably chooses the right tool and chains calls appropriately.

Purpose: Without system prompt guidance, Atlas may not know when to use `search_colleagues` vs `get_colleague_profile`, may not auto-chain on single results, and may duplicate card data in text responses. This plan ensures reliable tool routing.

Output: Updated `SYSTEM_PROMPT` in `openai_client.py` with a "Colleague Lookup" section containing rules 7-10.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\xmcp\.planning\PROJECT.md
@C:\xmcp\.planning\ROADMAP.md
@C:\xmcp\.planning\STATE.md
@C:\xmcp\.planning\phases\12-profile-card-frontend-system-prompt\12-RESEARCH.md
@C:\xmcp\.planning\phases\12-profile-card-frontend-system-prompt\12-CONTEXT.md
@C:\xmcp\chat_app\openai_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SYSTEM_PROMPT with Colleague Lookup section</name>
  <files>chat_app/openai_client.py</files>
  <action>
Modify the `SYSTEM_PROMPT` string in `openai_client.py` (currently lines 35-45). The existing rules 1-6 stay intact with two modifications:

1. **Update rule 1** to expand scope: Change "Only answer questions about Exchange infrastructure" to "Only answer questions about Exchange infrastructure or colleague lookups". Update the redirect message to include colleague lookups in the list of capabilities.

2. **Append a new "Colleague Lookup" section** after rule 6. The section contains:

```
## Colleague Lookup

You have two tools for finding colleagues:
- search_colleagues: Use when asked to find a colleague by name or email (e.g., 'find Jane Smith', 'who is alice@company.com?', 'look up Bob'). Returns up to 10 matches with name, title, department, and email.
- get_colleague_profile: Use when you have a specific email or user ID and want the full profile with photo. Requires a user_id parameter (accepts email address).

Rules:
7. When search_colleagues returns exactly 1 match, immediately call get_colleague_profile using that match's email as the user_id. Do not ask the user to confirm.
8. When search_colleagues returns multiple matches, present them as a numbered list showing name, title, and department. Ask the user which person they want the full profile for. Only call get_colleague_profile after the user identifies a specific person.
9. Never call get_colleague_profile speculatively or before you have a specific email/ID.
10. After get_colleague_profile succeeds, do NOT list the profile fields in your text response — the UI automatically renders a profile card. Respond briefly, for example: "Here's Jane Smith's profile." or "Found it — here's their profile card."
```

Key details:
- Keep the triple-quoted string format
- Existing rules 1-6 keep their numbering
- New rules are numbered 7-10 as a continuation
- Rule 10 is critical — it prevents Atlas from duplicating card data in text, which would look redundant with the DOM card
- The redirect message in rule 1 should mention colleague lookups alongside Exchange capabilities
  </action>
  <verify>
1. `grep -n "Colleague Lookup" chat_app/openai_client.py` returns the section header
2. `grep -n "search_colleagues" chat_app/openai_client.py` shows the tool description in the prompt
3. `grep -n "get_colleague_profile" chat_app/openai_client.py` shows the tool description and rules 7, 8, 9, 10
4. `grep -c "Rule" chat_app/openai_client.py` or count numbered rules — should have 10 rules total
5. `python -c "from chat_app.openai_client import SYSTEM_PROMPT; print(len(SYSTEM_PROMPT))"` runs without error (valid Python string)
  </verify>
  <done>
SYSTEM_PROMPT contains Colleague Lookup section with rules 7-10. Rule 1 updated to include colleague lookups in scope. Rule 7 handles auto-chain on single result. Rule 8 handles multiple results. Rule 9 prevents speculative calls. Rule 10 suppresses text duplication of card fields.
  </done>
</task>

</tasks>

<verification>
1. `python -c "from chat_app.openai_client import SYSTEM_PROMPT; print('Colleague Lookup' in SYSTEM_PROMPT)"` prints True
2. The SYSTEM_PROMPT string is valid Python (no unclosed quotes, no syntax errors)
3. Rules 1-6 are preserved unchanged (except rule 1 scope expansion)
4. Rules 7-10 cover: single-result auto-chain, multi-result listing, no speculative calls, no text duplication
</verification>

<success_criteria>
- SYSTEM_PROMPT includes "Colleague Lookup" section
- Rule 1 scope expanded to include colleague lookups
- Rules 7-10 added for tool routing guidance
- Rule 10 explicitly prevents text duplication of card fields
- Existing rules 1-6 preserved (rule 1 scope only change)
- Python module imports without errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-profile-card-frontend-system-prompt/12-02-SUMMARY.md`
</output>
