---
phase: 27-feedback-analytics-foundation
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - chat_app/openai_client.py
autonomous: true

must_haves:
  truths:
    - "System prompt contains Feedback Analytics section with guidance for get_feedback_summary and get_low_rated_responses"
    - "System prompt includes example queries for each feedback tool"
    - "System prompt includes rule that feedback analytics expose no per-user identity"
    - "System prompt includes rule that both tools default to last 7 days"
  artifacts:
    - path: "chat_app/openai_client.py"
      provides: "Feedback Analytics disambiguation section in SYSTEM_PROMPT"
      contains: "Feedback Analytics"
  key_links:
    - from: "SYSTEM_PROMPT in openai_client.py"
      to: "get_feedback_summary tool"
      via: "Intent-based routing rules"
      pattern: "get_feedback_summary.*satisfaction"
    - from: "SYSTEM_PROMPT in openai_client.py"
      to: "get_low_rated_responses tool"
      via: "Intent-based routing rules"
      pattern: "get_low_rated_responses.*negative"
---

<objective>
Add system prompt disambiguation for the two feedback analytics tools so the AI correctly routes feedback questions to the right tool.

Purpose: Without explicit guidance in the system prompt, the AI will not know when to use get_feedback_summary vs get_low_rated_responses, or that these tools exist at all. The disambiguation section follows the same pattern established in Phase 26 for message trace vs mail flow.
Output: Updated SYSTEM_PROMPT in openai_client.py with Feedback Analytics section.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-feedback-analytics-foundation/27-RESEARCH.md
@.planning/phases/27-feedback-analytics-foundation/27-CONTEXT.md
@.planning/phases/27-feedback-analytics-foundation/27-01-SUMMARY.md
@chat_app/openai_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Feedback Analytics section to SYSTEM_PROMPT</name>
  <files>chat_app/openai_client.py</files>
  <action>
    Add a new section to SYSTEM_PROMPT in chat_app/openai_client.py. Place it after the existing "## Message Trace vs Mail Flow" section (which was added in Phase 26, around line 81+). Follow the existing section pattern (## heading, descriptive text, numbered rules).

    Read the file first to find the exact last rule number used in the Message Trace section, then continue numbering sequentially.

    Add this section:

    ```
    ## Feedback Analytics

    You have two tools for querying Atlas feedback data:
    - **get_feedback_summary**: Returns aggregate vote counts (thumbs-up/down/total), satisfaction
      percentage, and daily trend breakdown. Use when asked: "How is feedback looking this week?",
      "What's our satisfaction rate?", "Show me feedback trends for the last month".
    - **get_low_rated_responses**: Returns individual thumbs-down entries with thread name, timestamp,
      and comment text (when provided). Use when asked: "Show me negative feedback", "What are users
      complaining about?", "Give me the thumbs-down responses with comments".

    [N]. When asked about overall feedback health or satisfaction rates, use get_feedback_summary.
    [N+1]. When asked to review specific negative feedback or user comments, use get_low_rated_responses.
    [N+2]. Both tools default to the last 7 days. For different periods, pass start_date and end_date as ISO 8601 strings.
    [N+3]. Feedback analytics show no per-user identity — all data is fully aggregate and anonymous. Do not attempt to identify who left specific feedback.
    ```

    Replace [N], [N+1], etc. with actual sequential rule numbers continuing from the last rule in the Message Trace section.

    Important:
    - Follow the exact formatting pattern of existing sections (indentation, line breaks, dash lists)
    - Do NOT modify any existing sections — only append the new section
    - The section should be inside the SYSTEM_PROMPT string, not outside it
  </action>
  <verify>
    Run: `cd /c/xmcp && python -c "from chat_app.openai_client import SYSTEM_PROMPT; assert 'Feedback Analytics' in SYSTEM_PROMPT; assert 'get_feedback_summary' in SYSTEM_PROMPT; assert 'get_low_rated_responses' in SYSTEM_PROMPT; assert 'no per-user identity' in SYSTEM_PROMPT; print('System prompt OK')"`
    Expect: "System prompt OK" with no errors.
  </verify>
  <done>
    SYSTEM_PROMPT contains Feedback Analytics section with tool descriptions, example queries, routing rules, date default guidance, and privacy rule. Rule numbers continue sequentially from the Message Trace section.
  </done>
</task>

</tasks>

<verification>
1. `python -c "from chat_app.openai_client import SYSTEM_PROMPT; print(SYSTEM_PROMPT)"` — Feedback Analytics section visible with correct tool names and rules
2. `grep -c "get_feedback_summary\|get_low_rated_responses" chat_app/openai_client.py` — both tool names appear in system prompt
3. `grep "no per-user identity" chat_app/openai_client.py` — privacy rule present
</verification>

<success_criteria>
- SYSTEM_PROMPT contains Feedback Analytics section following the established pattern
- Both tool names referenced with clear intent-based routing guidance
- Privacy boundary explicitly stated (no per-user identity)
- Date defaults documented (last 7 days)
- No existing sections modified
</success_criteria>

<output>
After completion, create `.planning/phases/27-feedback-analytics-foundation/27-02-SUMMARY.md`
</output>
