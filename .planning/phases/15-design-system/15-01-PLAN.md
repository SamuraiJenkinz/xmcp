---
phase: 15-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
autonomous: true

must_haves:
  truths:
    - "All --atlas- CSS custom properties are defined on :root (light defaults) and [data-theme='dark'] (dark overrides)"
    - "Tailwind @theme inline bridge exposes --atlas- tokens as tw: utility classes"
    - "@layer base sets html/body font-family, font-size, line-height, color, and background-color using --atlas- tokens"
    - "code/pre/kbd elements use --atlas-font-mono"
    - "font-optical-sizing: auto is set on html/body for Segoe UI Variable"
    - "Vite dev server starts without CSS parse errors"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "Atlas design token system, Tailwind bridge, base typography layer"
      contains: "--atlas-bg-canvas"
  key_links:
    - from: "frontend/src/index.css"
      to: ":root / [data-theme='dark']"
      via: "CSS custom property definitions"
      pattern: "--atlas-"
    - from: "frontend/src/index.css"
      to: "@theme inline"
      via: "Tailwind v4 theme bridge"
      pattern: "@theme inline"
---

<objective>
Define the complete --atlas- semantic token system in index.css: light/dark color tokens, typography tokens, Tailwind @theme inline bridge, and @layer base styles.

Purpose: Establishes the single source of truth for all visual styling in the React SPA. Every subsequent phase (16-19) consumes these tokens. No component styling in this plan -- only the token layer and base document styles.

Output: A fully populated `frontend/src/index.css` with ~120 lines of token definitions, theme overrides, base styles, and Tailwind bridge.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/index.css
@frontend/src/App.tsx
@frontend/src/main.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define --atlas- token system and base styles in index.css</name>
  <files>frontend/src/index.css</files>
  <action>
Replace the single-line `frontend/src/index.css` with the full Atlas design token system. The file structure must be in this exact order (Tailwind import first, then :root tokens, then [data-theme="dark"] overrides, then @layer base, then @theme inline):

1. **Tailwind import** (line 1): `@import "tailwindcss" prefix(tw);`

2. **:root light-mode tokens** (default/light values):
   - Surface hierarchy (3 tiers):
     - `--atlas-bg-canvas: #ffffff;` (colorNeutralBackground1 light)
     - `--atlas-bg-surface: #fafafa;` (colorNeutralBackground2 light)
     - `--atlas-bg-elevated: #f5f5f5;` (colorNeutralBackground3 light)
   - Text hierarchy:
     - `--atlas-text-primary: #242424;` (colorNeutralForeground1 light = grey[14])
     - `--atlas-text-secondary: #424242;` (colorNeutralForeground2 light = grey[26])
     - `--atlas-text-tertiary: #616161;` (colorNeutralForeground3 light = grey[38])
     - `--atlas-text-disabled: #707070;` (colorNeutralForeground4 light = grey[44])
   - Strokes:
     - `--atlas-stroke-1: #d1d1d1;` (colorNeutralStroke1 light = grey[82])
     - `--atlas-stroke-2: #e0e0e0;` (colorNeutralStroke2 light = grey[88])
   - Brand accent:
     - `--atlas-accent: #0f6cbd;` (brandWeb[80] light)
     - `--atlas-accent-text: #ffffff;`
     - `--atlas-accent-subtle: #ebf3fc;` (brandWeb[160])
   - Status colors:
     - `--atlas-status-success: #107c10;`
     - `--atlas-status-warning: #bc4b09;`
     - `--atlas-status-error: #c50f1f;`
   - Typography:
     - `--atlas-font-base: "Segoe UI Variable", "Segoe UI", -apple-system, BlinkMacSystemFont, Roboto, "Helvetica Neue", sans-serif;`
     - `--atlas-font-mono: Consolas, "Courier New", Courier, monospace;`
   - Font sizes (Fluent 2 type ramp):
     - `--atlas-text-caption2: 10px;`
     - `--atlas-text-caption1: 12px;`
     - `--atlas-text-body: 14px;`
     - `--atlas-text-body2: 16px;`
     - `--atlas-text-subtitle2: 16px;`
     - `--atlas-text-subtitle1: 20px;`
     - `--atlas-text-title3: 24px;`
     - `--atlas-text-title2: 28px;`
     - `--atlas-text-title1: 32px;`
   - Line heights (Fluent 2):
     - `--atlas-lh-caption2: 14px;`
     - `--atlas-lh-caption1: 16px;`
     - `--atlas-lh-body: 20px;`
     - `--atlas-lh-body2: 22px;`
     - `--atlas-lh-subtitle2: 22px;`
     - `--atlas-lh-subtitle1: 28px;`
     - `--atlas-lh-title3: 32px;`
     - `--atlas-lh-title2: 36px;`
     - `--atlas-lh-title1: 40px;`

3. **[data-theme="dark"] overrides** (only color tokens -- typography stays the same):
   - Surface hierarchy:
     - `--atlas-bg-canvas: #292929;` (grey[16])
     - `--atlas-bg-surface: #1f1f1f;` (grey[12])
     - `--atlas-bg-elevated: #141414;` (grey[8])
   - Text:
     - `--atlas-text-primary: #ffffff;`
     - `--atlas-text-secondary: #d6d6d6;` (grey[84])
     - `--atlas-text-tertiary: #adadad;` (grey[68])
     - `--atlas-text-disabled: #999999;` (grey[60])
   - Strokes:
     - `--atlas-stroke-1: #666666;` (grey[40])
     - `--atlas-stroke-2: #525252;` (grey[32])
   - Brand accent:
     - `--atlas-accent: #115ea3;` (brandWeb[70])
     - `--atlas-accent-text: #ffffff;`
     - `--atlas-accent-subtle: #061724;` (brandWeb[10])

4. **@layer base** styles:
   ```css
   @layer base {
     html, body {
       font-family: var(--atlas-font-base);
       font-size: var(--atlas-text-body);
       line-height: var(--atlas-lh-body);
       color: var(--atlas-text-primary);
       background-color: var(--atlas-bg-canvas);
       font-optical-sizing: auto;
       -webkit-font-smoothing: antialiased;
       margin: 0;
       padding: 0;
     }

     code, pre, kbd {
       font-family: var(--atlas-font-mono);
     }
   }
   ```

5. **@theme inline** Tailwind bridge (generates tw: utility classes):
   ```css
   @theme inline {
     --color-atlas-canvas: var(--atlas-bg-canvas);
     --color-atlas-surface: var(--atlas-bg-surface);
     --color-atlas-elevated: var(--atlas-bg-elevated);
     --color-atlas-primary: var(--atlas-text-primary);
     --color-atlas-secondary: var(--atlas-text-secondary);
     --color-atlas-accent: var(--atlas-accent);
     --color-atlas-stroke: var(--atlas-stroke-1);
     --font-atlas: var(--atlas-font-base);
     --font-atlas-mono: var(--atlas-font-mono);
   }
   ```

IMPORTANT NOTES:
- Do NOT remove the `@import "tailwindcss" prefix(tw);` line -- it must remain as line 1.
- The `:root` block must come BEFORE `@theme inline` so variables resolve correctly.
- Use CSS comments to organize sections clearly (e.g., `/* -- Atlas Semantic Tokens -- */`).
- All hex values above are verified from @fluentui/tokens@1.0.0-alpha.23 source. Use them exactly.
- Do NOT touch any other files. No React component changes.
  </action>
  <verify>
Run `cd /c/xmcp/frontend && npx vite build --mode development 2>&1 | head -20` to confirm no CSS parse errors. Then grep index.css for key tokens: `grep -c "\-\-atlas-" frontend/src/index.css` should return 40+ matches. Verify `@theme inline` block exists: `grep "@theme inline" frontend/src/index.css`.
  </verify>
  <done>
`frontend/src/index.css` contains all --atlas- token definitions on :root, dark mode overrides on [data-theme="dark"], @layer base with typography and background using tokens, and @theme inline bridge for Tailwind utilities. Vite build completes without CSS errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify token system renders correctly in dev server</name>
  <files></files>
  <action>
Run the Vite dev build to confirm no errors:

1. Run `cd /c/xmcp/frontend && npm run build` -- must complete with exit code 0 and no CSS-related warnings.
2. Verify the built CSS output contains --atlas- tokens: check `frontend_dist/assets/*.css` for `--atlas-bg-canvas` presence.
3. Run `cd /c/xmcp/frontend && npx tsc --noEmit` -- TypeScript must still pass (this plan touches no TS files, but confirm no regressions).

If build fails due to CSS parse errors, fix the index.css syntax (likely missing semicolons, unclosed blocks, or @theme inline ordering issues).
  </action>
  <verify>
`npm run build` exits 0. `npx tsc --noEmit` exits 0. Built CSS file in frontend_dist/assets/ contains `--atlas-bg-canvas`.
  </verify>
  <done>
Production build succeeds. TypeScript compilation clean. Built CSS bundle contains all Atlas tokens.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "\-\-atlas-" frontend/src/index.css` returns 40+ (all token definitions)
2. `grep "data-theme" frontend/src/index.css` shows dark mode override block
3. `grep "@layer base" frontend/src/index.css` shows base typography layer
4. `grep "@theme inline" frontend/src/index.css` shows Tailwind bridge
5. `npm run build` in frontend/ completes without errors
6. Built CSS in frontend_dist/assets/ contains --atlas-bg-canvas, --atlas-text-primary, --atlas-font-base
</verification>

<success_criteria>
- All --atlas- semantic color, text, stroke, accent, status, typography, and size tokens defined in index.css
- Light mode values on :root, dark mode overrides on [data-theme="dark"]
- @layer base sets document-level font-family, font-size, line-height, color, background-color from tokens
- @theme inline bridges select tokens to Tailwind utility classes
- Vite production build completes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/15-design-system/15-01-SUMMARY.md`
</output>
