---
phase: 15-design-system
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - frontend/src/components/AppLayout.tsx
  - frontend/src/components/ChatPane/Header.tsx
  - frontend/src/components/ChatPane/MessageList.tsx
  - frontend/src/components/ChatPane/InputArea.tsx
  - frontend/src/components/Sidebar/ThreadList.tsx
  - frontend/src/components/Sidebar/ThreadItem.tsx
  - frontend/src/components/ChatPane/AssistantMessage.tsx
  - frontend/src/components/ChatPane/UserMessage.tsx
  - frontend/src/components/ChatPane/ToolPanel.tsx
  - frontend/src/components/ChatPane/ProfileCard.tsx
  - frontend/src/components/ChatPane/SearchResultCard.tsx
  - frontend/src/components/ChatPane/MarkdownRenderer.tsx
  - frontend/src/components/shared/CopyButton.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
autonomous: false

must_haves:
  truths:
    - "Dark mode shows three-tier surface hierarchy: canvas (#292929), surface (#1f1f1f), elevated (#141414) -- verified by inspecting computed styles"
    - "Light mode shows three-tier surface hierarchy: canvas (#ffffff), surface (#fafafa), elevated (#f5f5f5) -- no dark-mode bleed"
    - "Toggling dark/light mode changes all surfaces, text, and strokes correctly"
    - "Body text renders in Segoe UI Variable (on Windows 11) at 14px/20px"
    - "Code blocks render in Consolas monospace"
    - "No hardcoded hex colors or ad-hoc grays remain in any React component"
    - "No ad-hoc className-based colors remain without corresponding --atlas- token CSS rules"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "Component-level CSS rules using --atlas- tokens"
      contains: ".app-container"
    - path: "frontend/src/components/AppLayout.tsx"
      provides: "Layout shell consuming atlas tokens via CSS classes"
      contains: "app-container"
  key_links:
    - from: "frontend/src/index.css"
      to: "React components"
      via: "CSS class names matching className props"
      pattern: "\\.(app-container|sidebar|chat-pane|chat-header)"
    - from: "frontend/src/index.css"
      to: ":root / [data-theme='dark']"
      via: "var(--atlas-*) references in all CSS rules"
      pattern: "var\\(--atlas-"
---

<objective>
Apply --atlas- tokens to all React component CSS rules: layout, sidebar, chat pane, messages, tool panels, profile cards, input area, and header. Establish the three-tier dark mode surface hierarchy and light mode neutral palette. Verify both modes visually.

Purpose: Makes the token system from 15-01 visible. Components that currently have no CSS rules (or inherit from legacy style.css) get proper --atlas- token-based styles. This is the visual foundation that Phases 16-19 build upon.

Output: Expanded `index.css` with component-level CSS rules using exclusively --atlas- tokens. All React components retain their existing className props with no markup changes needed (CSS rules target existing class names).
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-design-system/15-01-SUMMARY.md
@frontend/src/index.css
@frontend/src/components/AppLayout.tsx
@frontend/src/components/ChatPane/Header.tsx
@frontend/src/components/ChatPane/MessageList.tsx
@frontend/src/components/ChatPane/InputArea.tsx
@frontend/src/components/Sidebar/ThreadList.tsx
@frontend/src/components/Sidebar/ThreadItem.tsx
@frontend/src/components/ChatPane/AssistantMessage.tsx
@frontend/src/components/ChatPane/UserMessage.tsx
@frontend/src/components/ChatPane/ToolPanel.tsx
@frontend/src/components/ChatPane/ProfileCard.tsx
@frontend/src/components/ChatPane/SearchResultCard.tsx
@frontend/src/components/ChatPane/MarkdownRenderer.tsx
@frontend/src/components/shared/CopyButton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add component CSS rules using --atlas- tokens to index.css</name>
  <files>frontend/src/index.css</files>
  <action>
Append component-level CSS rules to `frontend/src/index.css` AFTER the existing @theme inline block. All rules MUST use `var(--atlas-*)` tokens exclusively -- no hardcoded hex values, no ad-hoc grays.

Add a `@layer components` block with rules for every CSS class used by React components. The surface hierarchy assignment is:

**Surface tier mapping (critical for visual hierarchy):**
- `--atlas-bg-canvas` (bg1): Main page background (html/body -- already set in @layer base), message list area
- `--atlas-bg-surface` (bg2): Sidebar background, input area background, header background
- `--atlas-bg-elevated` (bg3): Cards, tool panels, profile cards, elevated UI elements

**Layout rules:**
```css
@layer components {
  /* === Layout === */
  .app-container {
    display: flex;
    height: 100vh;
    width: 100%;
    overflow: hidden;
    background-color: var(--atlas-bg-canvas);
  }

  .sidebar {
    width: 260px;
    min-width: 260px;
    background-color: var(--atlas-bg-surface);
    border-right: 1px solid var(--atlas-stroke-2);
    display: flex;
    flex-direction: column;
    overflow-y: auto;
  }

  .chat-pane {
    flex: 1;
    display: flex;
    flex-direction: column;
    min-width: 0;
    background-color: var(--atlas-bg-canvas);
  }

  /* === Header === */
  .chat-header {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 8px 16px;
    background-color: var(--atlas-bg-surface);
    border-bottom: 1px solid var(--atlas-stroke-2);
    font-size: var(--atlas-text-caption1);
    color: var(--atlas-text-secondary);
  }

  .user-info {
    margin-right: auto;
    color: var(--atlas-text-primary);
    font-size: var(--atlas-text-body);
    font-weight: 600;
  }

  .theme-toggle-btn,
  .logout-btn {
    background: none;
    border: none;
    color: var(--atlas-text-secondary);
    cursor: pointer;
    padding: 4px 8px;
    border-radius: 4px;
    font-size: var(--atlas-text-body);
    text-decoration: none;
  }

  .theme-toggle-btn:hover,
  .logout-btn:hover {
    background-color: var(--atlas-bg-elevated);
    color: var(--atlas-text-primary);
  }

  /* === Thread List (Sidebar) === */
  .thread-list {
    display: flex;
    flex-direction: column;
    padding: 8px;
    gap: 2px;
  }

  .new-chat-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 8px 12px;
    margin-bottom: 8px;
    background-color: var(--atlas-accent);
    color: var(--atlas-accent-text);
    border: none;
    border-radius: 6px;
    cursor: pointer;
    font-size: var(--atlas-text-body);
    font-weight: 600;
    font-family: var(--atlas-font-base);
  }

  .new-chat-btn:hover {
    opacity: 0.9;
  }

  /* === Thread Item === */
  .thread-item {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 8px 12px;
    border-radius: 6px;
    cursor: pointer;
    color: var(--atlas-text-primary);
    font-size: var(--atlas-text-body);
  }

  .thread-item:hover {
    background-color: var(--atlas-bg-elevated);
  }

  .thread-item.active {
    background-color: var(--atlas-accent-subtle);
    color: var(--atlas-text-primary);
  }

  .thread-item-name {
    flex: 1;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .thread-item-name-input {
    flex: 1;
    background: var(--atlas-bg-canvas);
    color: var(--atlas-text-primary);
    border: 1px solid var(--atlas-stroke-1);
    border-radius: 4px;
    padding: 2px 6px;
    font-size: var(--atlas-text-body);
    font-family: var(--atlas-font-base);
    outline: none;
  }

  .thread-actions {
    display: flex;
    gap: 4px;
    opacity: 0;
    transition: opacity 150ms;
  }

  .thread-item:hover .thread-actions {
    opacity: 1;
  }

  .thread-action-btn {
    background: none;
    border: none;
    color: var(--atlas-text-tertiary);
    cursor: pointer;
    padding: 2px 4px;
    border-radius: 4px;
    font-size: var(--atlas-text-caption1);
  }

  .thread-action-btn:hover {
    color: var(--atlas-text-primary);
    background-color: var(--atlas-bg-canvas);
  }

  /* === Message List === */
  .chat-messages {
    flex: 1;
    overflow-y: auto;
    padding: 16px 24px;
    display: flex;
    flex-direction: column;
    gap: 16px;
    background-color: var(--atlas-bg-canvas);
  }

  /* === Messages === */
  .message {
    max-width: 800px;
    width: 100%;
    margin: 0 auto;
    font-size: var(--atlas-text-body);
    line-height: var(--atlas-lh-body);
    color: var(--atlas-text-primary);
  }

  .user-message {
    padding: 12px 16px;
    background-color: var(--atlas-bg-elevated);
    border-radius: 8px;
  }

  .assistant-message {
    padding: 12px 0;
  }

  .streaming-cursor {
    display: inline-block;
    width: 2px;
    height: 1em;
    background-color: var(--atlas-text-primary);
    margin-left: 2px;
    animation: blink 1s step-end infinite;
  }

  @keyframes blink {
    50% { opacity: 0; }
  }

  /* === Markdown Content === */
  .markdown-content {
    color: var(--atlas-text-primary);
    line-height: var(--atlas-lh-body);
  }

  .markdown-content h1,
  .markdown-content h2,
  .markdown-content h3 {
    color: var(--atlas-text-primary);
    margin-top: 16px;
    margin-bottom: 8px;
  }

  .markdown-content h1 { font-size: var(--atlas-text-title3); line-height: var(--atlas-lh-title3); }
  .markdown-content h2 { font-size: var(--atlas-text-subtitle1); line-height: var(--atlas-lh-subtitle1); }
  .markdown-content h3 { font-size: var(--atlas-text-body2); line-height: var(--atlas-lh-body2); font-weight: 600; }

  .markdown-content p {
    margin: 8px 0;
  }

  .markdown-content code {
    font-family: var(--atlas-font-mono);
    font-size: var(--atlas-text-caption1);
    background-color: var(--atlas-bg-elevated);
    padding: 2px 6px;
    border-radius: 4px;
  }

  .markdown-content pre {
    background-color: var(--atlas-bg-elevated);
    border: 1px solid var(--atlas-stroke-2);
    border-radius: 6px;
    padding: 12px 16px;
    overflow-x: auto;
    margin: 8px 0;
  }

  .markdown-content pre code {
    background: none;
    padding: 0;
    font-size: var(--atlas-text-caption1);
  }

  .markdown-content a {
    color: var(--atlas-accent);
    text-decoration: none;
  }

  .markdown-content a:hover {
    text-decoration: underline;
  }

  .markdown-content ul,
  .markdown-content ol {
    padding-left: 24px;
    margin: 8px 0;
  }

  .markdown-content table {
    border-collapse: collapse;
    width: 100%;
    margin: 8px 0;
  }

  .markdown-content th,
  .markdown-content td {
    border: 1px solid var(--atlas-stroke-2);
    padding: 8px 12px;
    text-align: left;
  }

  .markdown-content th {
    background-color: var(--atlas-bg-elevated);
    font-weight: 600;
  }

  /* === Tool Panel === */
  .tool-panel {
    margin: 8px 0;
    border: 1px solid var(--atlas-stroke-2);
    border-radius: 6px;
    background-color: var(--atlas-bg-elevated);
    overflow: hidden;
  }

  .tool-panels {
    display: flex;
    flex-direction: column;
    gap: 4px;
    margin-bottom: 8px;
  }

  .tool-panel-summary {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 8px 12px;
    cursor: pointer;
    font-size: var(--atlas-text-caption1);
    color: var(--atlas-text-secondary);
    list-style: none;
  }

  .tool-panel-summary::-webkit-details-marker {
    display: none;
  }

  .tool-panel-summary:hover {
    color: var(--atlas-text-primary);
    background-color: var(--atlas-bg-surface);
  }

  .tool-panel-icon {
    display: inline-block;
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background-color: var(--atlas-accent);
  }

  .tool-panel-name {
    font-weight: 600;
  }

  .tool-panel-status {
    margin-left: auto;
    font-size: var(--atlas-text-caption2);
    font-weight: 600;
  }

  .status-success { color: var(--atlas-status-success); }
  .status-error { color: var(--atlas-status-error); }

  .tool-panel-body {
    padding: 8px 12px;
    border-top: 1px solid var(--atlas-stroke-2);
  }

  .tool-panel-label {
    font-size: var(--atlas-text-caption1);
    color: var(--atlas-text-tertiary);
    margin-bottom: 4px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .tool-panel-json {
    font-family: var(--atlas-font-mono);
    font-size: var(--atlas-text-caption1);
    color: var(--atlas-text-secondary);
    background-color: var(--atlas-bg-surface);
    border-radius: 4px;
    padding: 8px 12px;
    overflow-x: auto;
    margin: 4px 0;
    white-space: pre-wrap;
    word-break: break-word;
  }

  .tool-panel-result {
    max-height: 300px;
    overflow-y: auto;
  }

  /* === Profile Card === */
  .profile-card {
    display: flex;
    gap: 12px;
    align-items: flex-start;
    padding: 12px 16px;
    margin: 8px 0;
    background-color: var(--atlas-bg-elevated);
    border: 1px solid var(--atlas-stroke-2);
    border-radius: 8px;
  }

  .profile-card-photo {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    object-fit: cover;
    flex-shrink: 0;
  }

  .profile-card-initials {
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: var(--atlas-accent);
    color: var(--atlas-accent-text);
    font-weight: 600;
    font-size: var(--atlas-text-body2);
  }

  .profile-card-info {
    display: flex;
    flex-direction: column;
    gap: 2px;
    min-width: 0;
  }

  .profile-card-name {
    font-weight: 600;
    font-size: var(--atlas-text-body);
    color: var(--atlas-text-primary);
  }

  .profile-card-field {
    font-size: var(--atlas-text-caption1);
    color: var(--atlas-text-secondary);
  }

  .profile-card-dept {
    font-size: var(--atlas-text-caption1);
    color: var(--atlas-text-tertiary);
  }

  .profile-card-email a {
    font-size: var(--atlas-text-caption1);
    color: var(--atlas-accent);
    text-decoration: none;
  }

  .profile-card-email a:hover {
    text-decoration: underline;
  }

  /* === Search Result Cards === */
  .search-results {
    display: flex;
    flex-direction: column;
    gap: 8px;
    margin: 8px 0;
  }

  .search-result-card {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: 4px;
    padding: 10px 16px;
    background-color: var(--atlas-bg-elevated);
    border: 1px solid var(--atlas-stroke-2);
    border-radius: 6px;
  }

  .search-result-name {
    font-weight: 600;
    color: var(--atlas-text-primary);
    font-size: var(--atlas-text-body);
  }

  .search-result-sep {
    color: var(--atlas-text-disabled);
  }

  .search-result-title {
    color: var(--atlas-text-secondary);
    font-size: var(--atlas-text-caption1);
  }

  .search-result-dept {
    color: var(--atlas-text-tertiary);
    font-size: var(--atlas-text-caption1);
  }

  .search-result-email-row {
    width: 100%;
  }

  .search-result-email {
    color: var(--atlas-accent);
    font-size: var(--atlas-text-caption1);
    text-decoration: none;
  }

  .search-result-email:hover {
    text-decoration: underline;
  }

  /* === Input Area === */
  .input-area {
    display: flex;
    gap: 8px;
    padding: 12px 24px 16px;
    background-color: var(--atlas-bg-surface);
    border-top: 1px solid var(--atlas-stroke-2);
    align-items: flex-end;
  }

  .chat-input {
    flex: 1;
    resize: none;
    border: 1px solid var(--atlas-stroke-1);
    border-radius: 8px;
    padding: 10px 14px;
    font-family: var(--atlas-font-base);
    font-size: var(--atlas-text-body);
    line-height: var(--atlas-lh-body);
    color: var(--atlas-text-primary);
    background-color: var(--atlas-bg-canvas);
    outline: none;
    min-height: 40px;
    max-height: 120px;
  }

  .chat-input:focus {
    border-color: var(--atlas-accent);
  }

  .chat-input::placeholder {
    color: var(--atlas-text-disabled);
  }

  .send-btn {
    padding: 8px 16px;
    background-color: var(--atlas-accent);
    color: var(--atlas-accent-text);
    border: none;
    border-radius: 6px;
    cursor: pointer;
    font-family: var(--atlas-font-base);
    font-size: var(--atlas-text-body);
    font-weight: 600;
    white-space: nowrap;
  }

  .send-btn:hover:not(:disabled) {
    opacity: 0.9;
  }

  .send-btn:disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }

  /* === Copy Button === */
  .copy-btn {
    background: none;
    border: 1px solid var(--atlas-stroke-1);
    color: var(--atlas-text-secondary);
    cursor: pointer;
    padding: 4px 8px;
    border-radius: 4px;
    font-size: var(--atlas-text-caption1);
    font-family: var(--atlas-font-base);
  }

  .copy-btn:hover {
    background-color: var(--atlas-bg-elevated);
    color: var(--atlas-text-primary);
  }

  /* === Loading State === */
  .loading {
    display: flex;
    align-items: center;
    justify-content: center;
    height: 100vh;
    color: var(--atlas-text-secondary);
    font-size: var(--atlas-text-body2);
    background-color: var(--atlas-bg-canvas);
  }
}
```

CRITICAL RULES:
- Every color/background/border MUST reference `var(--atlas-*)` -- zero hardcoded hex values in the @layer components block.
- Font sizes MUST use `var(--atlas-text-*)` and `var(--atlas-lh-*)` tokens -- no px values for font-size or line-height.
- Font-family MUST use `var(--atlas-font-base)` or `var(--atlas-font-mono)` -- no font name strings.
- The surface hierarchy is: canvas (bg1) for main content area, surface (bg2) for sidebar/header/input, elevated (bg3) for cards/panels.
- The above CSS targets ALL class names currently used in React components (verified by grep). No className changes in React components needed.
- Do NOT modify the :root, [data-theme="dark"], @layer base, or @theme inline sections created in 15-01.
  </action>
  <verify>
1. Run `cd /c/xmcp/frontend && npm run build` -- must exit 0.
2. Grep for any hardcoded hex in @layer components: `grep -A1 "@layer components" frontend/src/index.css | head -5` to confirm block exists, then visually scan for any `#` values not inside comments.
3. Count var(--atlas-) references: `grep -c "var(--atlas-" frontend/src/index.css` should return 80+.
4. Confirm all component class names have CSS rules: `grep "^\\.app-container\\|^\\.sidebar\\|^\\.chat-pane\\|^\\.chat-header\\|^\\.thread-list\\|^\\.message\\|^\\.input-area\\|^\\.tool-panel\\b\\|^\\.profile-card\\b\\|^\\.copy-btn\\|^\\.loading" frontend/src/index.css`.
  </verify>
  <done>
All React component CSS classes have rules in index.css using exclusively --atlas- tokens. Surface hierarchy correctly assigns canvas/surface/elevated tiers. Build passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete --atlas- design token system with component CSS. Dark mode three-tier surface hierarchy and light mode neutral palette applied to all React components.</what-built>
  <how-to-verify>
1. Start the dev server: `cd frontend && npm run dev` (ensure Flask is running on :5000 for API proxy)
2. Open http://localhost:5173 in the browser
3. **Dark mode check:**
   - Toggle to dark mode
   - Verify three distinct surface tiers are visible:
     - Canvas (#292929): Main chat message area background
     - Surface (#1f1f1f): Sidebar, header, and input area background
     - Elevated (#141414): Tool panels, profile cards, user message bubbles
   - Text should be white/light gray, not washed out
   - Borders between sidebar and chat pane should be visible but subtle
4. **Light mode check:**
   - Toggle to light mode
   - Verify canvas is white, surface is very light gray, elevated is slightly darker
   - No dark-mode values bleeding through
   - Text should be dark (#242424 primary)
5. **Typography check:**
   - Body text should be 14px Segoe UI Variable (or Segoe UI fallback)
   - Code blocks in assistant messages should use Consolas monospace
   - Heading sizes should follow the Fluent 2 type ramp
6. **Functional check:**
   - Theme toggle works without page reload
   - All existing functionality still works (send message, switch threads, tool panels expand)
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` in frontend/ completes without errors
2. `grep -c "var(--atlas-" frontend/src/index.css` returns 80+ (tokens used throughout)
3. No hardcoded hex values in @layer components block -- all colors via var(--atlas-*)
4. Dark mode computed styles show: body background #292929, sidebar #1f1f1f, tool panels #141414
5. Light mode computed styles show: body background #ffffff, sidebar #fafafa, tool panels #f5f5f5
6. Theme toggle switches all surfaces and text colors correctly
7. Body font renders as "Segoe UI Variable" on Windows 11 at 14px
</verification>

<success_criteria>
- Dark mode three-tier surface hierarchy matches Fluent 2 webDarkTheme (canvas/surface/elevated)
- Light mode uses Fluent 2 neutral palette with no dark-mode bleed
- All CSS rules use var(--atlas-*) tokens exclusively -- zero hardcoded colors
- Typography uses Segoe UI Variable + Fluent 2 type ramp sizes
- Human verification confirms visual correctness in both modes
</success_criteria>

<output>
After completion, create `.planning/phases/15-design-system/15-02-SUMMARY.md`
</output>
